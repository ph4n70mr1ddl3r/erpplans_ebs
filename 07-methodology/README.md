# 07 — Technical Methodology & Guidelines

> This folder contains technical implementation guidelines and reference specifications
> for the BuildRight Depot Corp. ERP system. It is intentionally minimal at this stage;
> detailed methodology documents (implementation approach, testing strategy, change management)
> will be added during platform selection and implementation planning.

## Contents

| Document | Description |
|---|---|
| [technical-guidelines.md](technical-guidelines.md) | POS hardware specs, infrastructure & deployment reference, integration architecture (reference copy; canonical diagram lives in `01-model-company/data-volumes-and-integrations.md`), security requirements, two-tier sourcing-architecture reference (§5) |
| [capability-sourcing-and-engineering-model.md](capability-sourcing-and-engineering-model.md) | Two-tier capability-sourcing doctrine (2026-09-14; supersedes the 2026-09-03 hybrid posture) — use-EBS/build decision gate (Sourcing & Investment Board), Capability Sourcing Register (in-suite Oracle EBS incl. Oracle WMS/MSCA + Shipping/OTE + in-house/already-built products incl. Payroll PH and the POS estate), build-squad shape & engineering standard, Software Engineering Platform (SEP), and the **Agentic Automation Program** (§12: autonomy ladder, agent lifecycle, crawl-walk-run) |
| [it-product-operating-model.md](it-product-operating-model.md) | IT product operating model (v3.23, two-tier + agentic — the IT department's structure of record) — 17 product teams (9 stream-aligned domain incl. OMO/TPS build squads + 7 platform incl. SEP and the AI & Agent Platform + CIO Office) covering all 188 value streams; membership, roles, RACI, governance, sizing (122 FTE) |
| [ai-first-operating-guide.md](ai-first-operating-guide.md) | The AI-first operating guide (v1.16, 2026-09-23) — the complete, ERP-agnostic, sourcing-neutral operating playbook synthesized for adopters: ten operating laws; the six-layer reference architecture on the knowledge plane; capability-contract doctrine & core portability test; the Tier-wired autonomy ladder & hard boundaries; the agent factory (paved road, Agent Charter, lifecycle, evaluation); the Enterprise Knowledge Base as persistent memory (canon, citation-or-refusal read rule, write path, consistency engine); the no-confusion decision system (ownership rule, decision rights, Universal Decision Procedure, escalation clocks, situation playbooks); governance cadence & KPIs; and the seven-phase adoption path for similar companies |
| [validate-repo.sh](validate-repo.sh) | Cross-reference validation script — 78 checks covering workflow counts, requirement/control IDs, classification register, dangling workflow references, markdown table structure, analysis-field-header canonicalization, PA-footer format + re-derived footer figures (the Check-16 figure arm, twenty-seventh wave: every footer's Workflow Count re-derived against the file's own ## W headers, the VS-number against the folder, and the VS-name against the canonical value-stream-index summary row), orphan-workflow-body (ghost) detection, criticality-classification prose-count vs heading consistency, boilerplate/tier-1 chain consistency (+ the 2026-09-17 fiftieth-wave Pain-Points arms inside Check 10: the 27 retired Expansion-rework template literals probed verbatim, the corpus-wide verbatim-sharing arm forbidding any Pain bullet text in more than one workflow, and the 5,432-workflow parse-population assertion — after the wave found the 2026-06-20 rework's own replacement paste running to 152 identical copies of the Data-quality bullet across the seven Python-assisted value streams and re-authoring 194 workflows across 42 PA files), value-stream-index PA/VS link resolution, PA-file relative-link resolution, Automation/Controls draft-field content quality, required-field (9-field) completeness, intra-file TOC anchor resolution, workflows/README.md family reconciliation + stale canonical-figure guard, criticality proposed-register mirror reconciliation + stale unclassified-figure guard, touchpoint-map self-declared reconciliation guard (+ the 2026-09-17 forty-eighth-wave §2-intro arms: the live population figure and 114-VS span re-derived against the VS-79..max PA ## W headers every run with the v74.0 authoring figure pinned as history, and every §2 module cell resolved to a module-row heading through the documented nine-alias set with the 36-row module population and the intro's convention anchors asserted), dependency-map §8 block reconciliation guard, stale register-row figure / unclassified-workflow-claim guards, requirements-TOC letter-suffixed-ID / proposed-register-description guards, repo-wide table delimiter/data-row column integrity (GFM delimiter-vs-header, code-fence & escaped-pipe aware, all .md files incl. the 569 PAs), PA-file TOC completeness/duplicate/stray-fragment hygiene (h2 workflows indexed only, per WORKFLOW-FORMAT-GUIDE convention), PA-name 3-way consistency (PA-file H1 ↔ VS-README row ↔ index bullet, canonical = index) with the 188 VS-README H1 'VS-nn: Name' titles pinned to the same index canon (the H1 arm, twenty-seventh wave), the 569 PA header quote-lines pinned to the same canon over a line-break-proof joined read with VS-number, optional canonical family suffix and Value-Stream-Index link verified, and the 188 detailed-map '**[VS-nn: Name]**' bullets pinned to the summary-row name (the quote-line and detailed-bullet arms, twenty-eighth wave — fourteen drifted quote-line VS names across VS-48/100/114/130/192, twelve of them in files whose footers wave 27 had just trued, plus the VS-192 detailed bullet that wave's clean adjudication missed), workflow-catalog requirement-ID citation resolution (every cited ID must exist as an erp-requirements.md row; standards/law/project-code false positives exempted), workflow-reference resolution inside PA bodies & VS READMEs (every `W…` token must resolve to a defined workflow header — Check 7 guards only the four cross-reference docs), PA-control objective canonical-name agreement (all 569 CTL-240–808 objectives on both the register and PA-body citation surfaces must carry the canonical value-stream-index PA name), VS-number citation resolution (every `VS-<n>` token must denote an active value stream — catching workflow references written in the VS namespace, e.g. "`VS-469` DTI" where W469 is the DTI-escalation workflow), duplicate-requirement-title guard (no capability registered twice under different IDs), requirement priority-split figure agreement (every quoted 'N Must / M Should / K Nice' triple must match the register), requirements-TOC count-column agreement (per-section Counts vs defined register rows; Total-line vs register size), repo-wide namespace & listing integrity (unique W-headers; CTL-XX and PA-N.N citation resolution; cross-file anchor resolution), validator self-description agreement (every quoted check-count figure must equal the checks implemented in this script), and quoted requirement-total/category-count agreement (every 'N requirements' / 'N categories' total-claim must match the register), and duplicate workflow-title guard (no two `##` workflows may share a normalized title — Check 36's workflow-side counterpart; three parallel program-template groups are adjudicated intentional on the check's allowlist), and analysis-section list hygiene + bold/paren balance (every Controls-section line must be a bullet or indented continuation, Controls bullets must carry balanced parens and no stray `**` artifacts, and every paragraph block in the model-company docs must have an even `**` count with code spans stripped), and root-README folder-tree agreement (every VS-directory row's workflow/PA counts, the workflows/ total line, and the proposed-register description row's unclassified count & W-ID range must agree with value-stream-index.md and workflow-criticality-proposed.md), and anchored current-state total-figure agreement (the format-guide completeness/100%-presence/Layout/Related-Documents claims, the requirement-matrix 'full N-workflow inventory' line, the VS-133 README & PA-133.1/.3 Volume self-descriptions, and the dependency-map VS-133 row must quote the index grand total, the classification confirmed-row count, and the proposal register's unclassified complement & ID range), and stale-figure & superseded-citation literal guard (the retired literals from the review #29 semantic-drift repairs plus the review #31/#32 statutory-sweep extensions — stale e-wallet tender-mix bases/totals, the 1,000-terminal fleet figure, the 80,000-SKU volume, and superseded statutory forms (LPG act RA 10862 → RA 11592; Philippine Competition Act RA 10677 → RA 10667 — the number review #31 flipped and review #33 restored; hazardous-waste DAOs incl. the spelled-out `Administrative Order No. 2013-22` → DAO 2004-36 under RA 6969; phantom chemical-safety orders DAO 2015-09 → DAO 2004-36, DAO 2015-08 → RA 6969 + implementing rules, DAO 2021-19 → RA 6969/DAO 2004-36, DAO 2005-10 → Sanitation Code PD 856 Ch. XVII / National Plumbing Code; DOLE Department Order 1989-114 → OSH Standards per D.O. 198-18; transfer pricing RR 34-2022 → RR 02-2013; 13th-month-pay RA 10691 → PD 851) plus the phantom-ATC and construction-OSH forms plus the review #33 statutory-sweep forms (ADR-Act RA 876 label, age-restricted-sales RA 7610 pairing, D.O. 53-03, RR 13-2018 SAF-T, PD 1586 release-notification pairing, EVIDA-IRR DAO 2023-05) — must not reappear in current-state prose; CHANGELOG and 'X → Y' change-notes exempt), and per-store-rate/chain-total unit coherence & store-multiplication math (every same-line pairing of a 'N–M per store per week\|month\|day\|year' rate with a '~A–B …/unit chain-wide' total must cohere once normalized to events/month/200-store chain — effort- and weight-denominated totals exempt; and every explicit single-multiplier '200 stores × N = M' product must check out raw or cadence-normalized), and steps-table row integrity & step-ID ordering (every steps table carries the canonical '\| # \| Activity \| Role (R) \| Role (A) \| Duration\|Frequency\|Latency \|' header, no step row loses its leading table pipe, and step IDs ascend (num, letter) with letter variants after their base row — the review #30 repair class for the '6a \|'/'16c \|' bare rows, the 'Role (R))'/'Role(A)' header typos, and the W922/W35/W9 mis-ordered rows), and Time-Estimate/Staffing inline-arithmetic guard (the strict guard mode of audit-time-estimate-math.py re-derives every explicit 'A × B = C' chain in the finalized Time Estimate and Staffing Implication paragraphs under the licensed unit conventions — min↔hours, sec→hours, workday/workweek/month alternates, cadence annualization, 200-store/4-DC chain scalings, noun/suffix cancellation — erroring on single-run effort-time chains whose midpoint is off ≥1.6× under no licensed convention and on reversed numeric ranges; the 2026-08-29 pass repaired 25 workflows' defective chains), and staffing-claim & Volume-product reconciliation (guard mode of reconcile-staffing-claims.py — retired literals from the review #34 repairs: pre-rebalance department totals (IT ~28→50, Finance 37→46, Legal ~9→14, Store Ops ~23→24), stale role counts (3→4 Pricing Analysts, 10–12→10 Buyers, 6→5 Category Managers, 2→5 Merchandise Planners), the per-shift DC-worker phrasing vs the canonical 4 × 150 = 600, and the DSD per-store week→month cadence; plus a positive '<Department> team of N' equality check against the §3.3 register with engagement-crew exemptions, and elementwise verification of every explicit ×-product in Volume/Frequency field rows with K/M endpoints and week→month allowance), and ST-vocabulary & duplicate-Trigger guard (guard mode of audit-st-touchpoints.py — the eight rogue spelling/hyphenation variants normalized by review #35 (ageing→aging incl. the W995 title/TOC/register cascade, drilldown→drill-down, pick-up→pickup, put-away→putaway, time-stamp(ed)→timestamp(ed), charge-back(s)→chargeback(s), anti-counterfeiting→anti-counterfeit, self-serve→self-service) must not reappear, and any same-PA byte-identical Trigger pair not on the 20-cluster adjudicated shared-event allowlist is an error; title-canonical forms like W258 Omni-channel and W3657 Closeout are deliberately preserved), and Automation-keyword & RACI role-title guard (--check mode of fix-auto-keywords.py — the defragmented auto-bullet keyword glitches (mid-word clips, legal-entity captures, trailing fragments, nested quotes) and cell-bounded role-title variants against the dominant forms (incl. the org-chart ghost VP Communications, which section 11.1 does not list) must not reappear), and risk-label / cadence / owner vocabulary guard (guard mode of audit-field-vocabulary.py — thirteen retired Pain-Points risk-label variants, the unhyphenated 'ad hoc' cadence form, and the EHS-Manager spelling must not reappear; the bare 'Compliance Manager' title is adjudicated legitimate), and Participants hygiene & per-unit volume coherence (guard mode of audit-participants-units.py — no RACI markers in Participants rows, no 'Analytics Mgr' abbreviation, and any same-row pairing of a chain-wide DC/store total with a per-unit figure must satisfy units x per-unit within ±30% — the W3 1,200/DC vs 6,000 class), and operational-control prose variant guard (guard mode of audit-operational-controls.py — the hyphenated 'review-and-approval gate', 'review & approval', 'VP for X' and qualifier/gloss forms retired by review #39 must not reappear), and risk-label punctuation guard (guard mode of audit-risk-labels.py — the em-dash '**X risk** —' form retired by review #40 must not reappear), and mitigation-clause & Trigger-richness completeness (guard mode of audit-enrichment-completeness.py — every risk bullet must carry mitigation semantics under the refined net, and no cadence-only Trigger may appear outside the Check-52 shared-event allowlist), and root model-doc integrity (guard mode of audit-model-docs.py — every W/VS/CTL/PA/requirement token in mobile-app-strategy.md, data-migration-mapping.md, assumptions-and-design-decisions.md and — since review #68 — the adopted target-state TO (optimal-table-of-organization.md, incl. a structural rule re-deriving every §7.3 DC-roster group total from its own HC cells and the two-state 362/6,762 → 532/6,932 anchors) and the IT product operating model (it-product-operating-model.md, incl. the ten-cluster integration anchor and the 171+17=188 / 4,868+502=5,370 reconciliation sums) must resolve against the live registers, §-refs must resolve doc-scoped, and the retired stale totals and review-#68 retired literals (the 51-outbound roster label, the 11-cluster count, the §13.3 seasonal-calendar §-ref) must not appear; the AI-first operating guide (ai-first-operating-guide.md) joined the doc set 2026-09-04 — verified clean on dry-run — with a guide_figure_hits structural rule re-deriving its catalog triple (both prose and compressed forms), the Tier-wired autonomy-ladder sentence, and the control/requirement canon-table citations from the primary registers), and exec-summary anchor & CTL citation-scope guard (guard mode of audit-exec-ctl.py — executive-summary.md must carry the canonical anchor figures with retired totals absent outside change-notes, and CTL-01/CTL-02 citations must carry spend-scoped application notes), and matrix ghost-row & summary-doc anchor guard (guard mode of audit-matrix-refs.py — no requirement-matrix row may map only to ghost W aliases, the gap-analysis current-state line must quote the canonical totals, and technical-guidelines must carry its verified anchor figures), and semantic-sample anchor guard (guard mode of audit-semantic-anchors.py — the four literals repaired by the 64-workflow semantic sample of review #44 are retired, and Participants-field counts for the section 13.1 merchandising roles must equal the register), and root-README worklist-row & methodology-tree guard (Check 63, added by review #69 — the batch17/batch18 tree rows must agree with the worklist files' own headers: the stale 'residual: W712 phrasing, W16.1 approval-matrix design' form retired, the review-#64 W24-ladder closure anchor required, the '~N of ~M spots repaired' and '(~N spots' figures matched against the files, and every 07-methodology file must be listed in the root-README folder tree), and event-custody register guards (Check 64, added by the 2026-09-03 event-custody pass — the dependency map may carry no parent→itself self-loop edge (the W54→W54 / W288→W288 degenerate class, now re-homed as CIRC-005 or repaired to the intended soft edge), and every overlap pair declared in `event-custody-and-precedence-register.md` §4 (`VS-A ↔ VS-B`) must cross-reference bidirectionally across the pair's workflow files so a declared custody split can never ship one-sided; fifty-ninth-wave cycle arm, 2026-09-21 — the map may also carry no cross-block hard-prerequisite cycle: DFS over the '→' edge grammar reports any cycle with its full path and line number ('⇢' soft and '↔' bidirectional edges exempt by type), closing the W31 ⇄ W312 class after §1.1's 'W312 → W31 (parameter inputs)' back-edge was found contradicting §5's own W31 → W312 chain — an undeclared hard 2-cycle stranded from the initial commit that the self-loop arm could not see; W312's own file reads 'forecast feeds parameter calculation', so the tree edge was the contradicted direction and was removed, the true one-way analytical feed remaining declared in §5 and in W312's own file, no CIRC row needed), and cross-VS duplicate-event guard (Check 65, added by the 2026-09-03 second custody wave — byte-identical event Triggers and byte-identical distinctive Frequency canons shared across value streams are errors outside the 33 adjudicated shared-cadence/shared-canon clusters; pure-cadence triggers auto-exempt; the paraphrased-duplicate class is covered by the event-custody register's scope notes), and repo-wide file-level relative-link resolution outside PA files (Check 66, added by the 2026-09-03 residual sweep — every relative markdown link target in the non-PA .md files [VS READMEs, workflows/ support docs, root docs, methodology docs] must resolve to an existing file; Check 20 guards PA files and Check 39(d) the anchor half; added after the VS-08/VS-32 READMEs shipped links to the event-custody register missing the '../' prefix), and VS-README process-area table cross-foot (Check 67, added by the 2026-09-03 consistency review pass — every VS README's Process-Areas table rows and **Total** row are re-derived from the PA files' own `## W` headers; added after the worklist-adjudication pass moved W239 from PA-24.3 to PA-87.3 and re-pointed the index/root-README/dependency-map but left both VS READMEs' tables stale), and value-stream-index per-VS row cross-foot (Check 68, added by the 2026-09-03 second consistency review pass — the 'Value Stream Architecture' summary-table rows' per-VS Process-Areas/Workflows cells and the detailed section's per-VS '(n workflows)' headings are re-derived from the PA files' own `## W` headers, with missing rows for on-disk VSs and phantom rows for non-existent VSs both erroring; added after the 2026-08-26 batch-4 additions (W5508 into VS-79.2, W5509 into VS-02.1) trued the summary-table cells, VS READMEs and Grand Total but left both headings stale — VS-02 '37 workflows' vs disk 38, VS-79 '24' vs 25 — on a surface no check read), and semantic-audit registry integrity (Check 69, added by the 2026-09-04 consistency review pass — the bare W-id lines of semantic-audit-coverage.txt, from which final-semantic-coverage.py derives its detector audited-set, must be distinct, resolve to live `## W` workflow headers, never contradict a '(transition-path pending)' note, and close the set equation live workflows = admitted + pending-named with en-dash ranges expanded; added after the batch-9 gap-fill appended its four workflow ids W5525–W5528 as bare lines directly above its own 'NOT yet in the audited registry' note, silently widening the audited-set to 5,374 — neither the closed-loop 5,370 nor the live 5,387) — and audit-model-docs.py (Check 59) gains register_heading_hits, which re-derives every '(n Workflows)' parenthetical in the criticality register from its own data rows (nested sub-heading rows roll up), the three '## Tier N' heading claims and the ## Summary per-phase counts must equal the effective per-tier row totals (rows → headings → Summary chain closed; the pass trued the Tier-2 Additions sub-heading stragglered at 495 by the two 2026-09-03 gap-fill passes), and true three stale live-body version pins in the guarded docs (the sourcing-model header floor pin and its §13 Related-Documents OM pin re-pinned to the OM's current footer version, and the OM's §13 'this v2.0 model' self-pin re-pinned to v2.4), and generated-tree structural validation (Check 71, added by the 2026-09-05 seventh-wave consistency review — the bpmn/ and dmn/ trees, previously read only by the generators' self-checks on manual regeneration, are re-derived structurally against the markdown corpus on every run: well-formed XML, one .bpmn per PA file and one process per confirmed-register row (5,450), 79 decisions across the dmn/ tree, per-process start/end cardinality, full lane coverage with in-process lane refs, 1:1 BPMNDiagram:BPMNPlane:process, DI shape/edge coverage with positive bounds and waypoints, decision-table structure, and a DRD shape per decision), and document version-chain monotonicity (Check 72, added by the 2026-09-07 eleventh-wave consistency review — every '*Document Version:' footer's 'Prior vN.M' chain must be strictly decreasing with no prior at or above the live version, repo-wide across all versioned docs; added after the AI-first operating guide was found carrying all eight batch-16–23 re-point clauses labeled v1.1–v1.8 beneath a version header that never left '1.0 \| Initial issue' — an inversion no rule read because the index pin satisfied itself off the same stale footer; the guide's footer was re-based newest-first to live v1.9 with the initial issue demoted to the chain's end), and canonical integration-diagram twin-copy byte-identity (Check 73, added by the 2026-09-07 twelfth-wave consistency review — the ASCII integration-architecture diagram's canonical copy in data-volumes-and-integrations.md and its declared convenience duplicate in technical-guidelines.md §3.2 must stay byte-identical; waves 7/9/11 had each re-verified the twin copies by hand), and generated-tree coverage-surface agreement (Check 74, added by the same twelfth wave — the bpmn/README and dmn/README quick-stats tables and the generated-tree figures quoted on the root-README tree/generator rows and the exec-summary tree rows are re-derived from the shipped XML by the generators' own task/flow/decision/rule definitions every run, with the dmn/README deferred-rule-set anchor (197) pinned), and the official-TO register arithmetic (added by the 2026-09-09 thirteenth-wave consistency review as to_register_hits inside audit-model-docs — the review trued the v2.0 register issuance's own self-descriptions: footer '≈150 distinct roles' → the register's true 188 rows / 187 distinct titles, span note 'Controller at 7 directs' → 7 sub-team lines / 8 register reporting rows in-band, §5.2 GL & Consolidation mix cell conformed to its register rows; the rule re-derives §5.1's 18-department current/target columns and two-state total rows, the three §5.2 sub-team tables, every §5.3 department-table HC foot, the §5.1↔§5.3 pairing, the register-total footer line, and both quoted figures every run), and the fourteenth-wave additions (2026-09-09) — Check 27 Part C (PA-corpus live-total guard: every interim unique-workflow total in the retired 5,320–5,425 band must not appear workflow-adjacent in any PA file's live prose, '*Date:' footer lines exempt; added after PA-128.3's W5512 Volume row was found quoting the '5,370-workflow Automation Opportunity inventory' three batches after the corpus had grown to 5,426 — the gap-fill batches re-pointed every navigation surface each move but no check read the PA field rows; the '~5,400 trade/corporate accounts' canon never matches since the rule requires workflow adjacency), Check 74 extension (the methodology-index generator rows are a fourth sibling surface quoting the tree figures — 569 files / 5,450 processes and 40 files / 79 decisions / 339 rules — now re-derived from the shipped XML every run alongside the root-README/exec-summary rows, with the 197 deferred-rule-set anchor pinned on the generate-dmn row), and Check 75 (methodology-index Contents completeness vs disk: every on-disk 07-methodology/ tool (.py) and doc (.md, the index itself excluded) must carry a Contents row in 07-methodology/README.md and every Contents-row link must resolve — closing the completeness gap the twelfth wave documented when it completed the table by hand and recorded that no check read it; the six .txt worklist/census data artifacts are out of scope, listed only in the root-README tree per Check 63; the repair side of the wave: PA-128.3's Volume row trued 5,370 → 5,426), and Check 76 (domain gap-analysis companion figures: the four batch-8–11 companions workflow-gap-analysis-finance/it/operations/people.md — live navigation surfaces referenced from the gap history, classification register, touchpoint map, executive summary, IT operating model and sourcing model, yet read by no check — now have every family roster, bucket total, inventory clause, adjacent-list count and 'now stands at' figure re-derived every run from the canonical family tables and the PA files' own ## W headers, with membership pinned in both directions and every W-token resolved; the fifteenth wave trued their authoring-time snapshots stranded by the later gap-fill batches — finance 30 VS / 775 → 29 / 785 with VS-72/VS-158 un-mislisted as adjacent, IT 9 VS / 284 → the canonical 13 / 390, operations' five buckets trued to 462/500/1,545/323/976 behind its 'four families' slip, people 437→440 → 445 with VS-183 added to its roster), and the sixteenth-wave additions (2026-09-09) — Check 51 extended and armed (the DC-catchment class: the per-DC catchment average re-derived from the profile's §3.1 Total-Stores and §3.2 DC-table rows every run — 200 ÷ 4 = 50, range canon 20–80 per the region/DC-role tables — with the retired '~40 on average' / '(range: 20–60)' / '~40–50 stores' forms retired after the profile's own operations bullet was found contradicting its DC3 row and PA-26.1 cited the profile with the right range but the stale average; and the guard now actually passes --guard, the error branch having been dead code that let two latent volume-product checker false positives print invisibly — fixed by whole-row '+'-sum skipping and parenthetical-annotation stripping), and Check 59's profile_derived_figure_hits (the revenue-per-employee quotient re-derived from §9.4 ÷ §4 every run with every live citation swept — 9.22M stranded at the pre-rebalance denominator on the profile §4 note, assumptions A2.6 and PA-133.3's Volume row — and the B2B account canon re-derived from §9.2 with §10.3/§15.2 cross-footed and the retired pre-canonicalization trade-account literals swept from live prose after the v2.23 canonicalization left §15.2 and PA-11.2 behind), and the eighteenth-wave additions (2026-09-10) — Check 39 Part E (the misdirected-CTL guard: audit-misdirected-ctl.py --guard re-derives the census every run — zero paren-form citations naming a different PA (the unambiguous wrong-PA defect class the batches repaired), and the batch-13 hand-adjudicated 170-citation isolated colon-form population pinned at total/per-PA/per-cited-CTL granularity so any new paste, re-map or deletion forces a conscious re-adjudication with the baseline re-pointed; arming was possible only because the same wave re-pointed the script and seven sibling tools off a hardcoded /home/riddler absolute path that had left them unrunnable outside the original author's machine since authoring, with fix-ghost-roles-batch26 additionally sunset-guarded after its sandbox run rewrote the official TO's deliberately-charted 'VP Finance & Accounting'/'VP HR' sub-seats — its rules predate the chart), and the twentieth-wave addition (2026-09-14) — Check 77 (sourcing-doctrine posture guard: the two-tier doctrine enactment re-pointed every guarded surface while stranding the posture prose on live surfaces no rule read — the executive summary's 'unified cloud ERP core provided by a theoretical software vendor' intro and 'Bought Edges' landscape table, the guide's §4.2 'The bought edge systems (WMS/TMS/WFM/FSM)' and 'payroll stays statutory-core' assignment rows and its L3 'bought best-of-breed edges' layer line, the root-README OM tree row's 'hybrid: ERP core + BoB edges', the sourcing model's own §1 'best-of-breed lifecycle' list item, the VS-113 sourcing machinery's retired three-way 'configure → buy → build' gate prose, and the operational PA bodies' 'unified cloud ERP' descriptions; the rule retires the 'cloud ERP' literal repo-wide in live prose (the sourcing-model §1 v1.x history clause, dated change-notes and the gap-analysis scenario table exempt), retires lowercase 'best-of-breed' in the VS-113 folder and PA-128.3 (the W5516 registered title keeps its case), guard-anchors the executive summary's two-tier landscape anchors and the assumptions A6.3 already-built row, and pins the guide's two repaired assignment phrases against regression), and the twenty-second-wave additions (2026-09-14) — Check 77 hardened: the IT gap-analysis companion's §1 scope line shipped the retired 'hybrid cloud ERP + best-of-breed edge + 2 in-house built products' three-tier posture and escaped BOTH sweep arms (the 'cloud ERP' literal was split across a line break, invisible to the line-based probe; the best-of-breed sweep was scoped to VS-113 + PA-128.3) — repaired in place to the two-tier canon and the class closed: the 'cloud ERP' probe is now joined-text (line-break-proof, offset→line mapping preserved so a firing guard still points at the exact line), the lowercase-best-of-breed sweep extends to the four domain gap-analysis companions (PA-138.1's 'single-source IFM vs best-of-breed' adjudicated out of scope — outsourced-facilities-services vendor strategy is commodity procurement under the doctrine, not capability-product sourcing), and the companion's repaired scope anchor is required present; and the twenty-third-wave additions (2026-09-14) — Check 77 extended: the cascade's own teaching surfaces still taught the retired three-exit gate after wave 20 trued W5515/PA-128.3 (the sourcing model's §2 heading 'Unified Core, Bought Edges, Built Differentiators' + 'three tiers' intro over its own two-tier table and §12.2's 'vendor agent products = buy' routing; the guide's Law 4 'in that default order' statement, §3.3's 'exactly three sourcing postures' estate with a '**Bought edge**' row holding the retired BoB rows, §5.1's 'configure → buy → build', §7.3's 'buy = vendor agent' intake routing and §9.2's '(configure/buy/build)' decision-rights row; the OM's §2 principle 7 'Configure by default; buy before build' with the stale four-item appendix list and §8's 'Configure/buy/build routing' SIB row; PA-128.3's W5512 Step 3 'vendor agent product = buy' two paragraphs above its own use-EBS→build bullet) — all repaired in place to the two-exit canon, the retired gate-order literal joins the joined-text probe (arrow form only; the sourcing-model §1 history narration and the gap-analysis batch records stay exempt), and parts (h) anchor-pin every repaired surface; and the twenty-fourth-wave additions (2026-09-14) — Check 77 extended (part (i)): the cascade's own portfolio/decision-rights/enumeration surfaces, one layer out from the wave-20/23 trues — the OM §3.2 portfolio-table Type cells still read 'Stream-aligned (buy & integrate)' (rows 2–4) against the same document's §5.1 'configure-and-integrate teams (WLI, SSP, CCP)', the OM §6.1 RACI row still routed 'Capability sourcing decision (configure/buy/build)', the W5515 Controls bullet still enumerated the retired 'exit/run plan' appendix against §3.3's 'Run-cost & talent plan (build)' and the wave-20-trued Step 2, the sourcing model's §9 TCO-per-product row still carried the retired BoB-product cost class ('BoB products carry license + integration run cost' — a v1.0 relic the v3.0 re-word claim never covered), and the guide's §5.1 five-appendix enumeration kept the retired 'exit/run-cost plan' name — all repaired in place and anchor-pinned (joined live text; the retired literals forbidden per document, the guide's §5 posture-neutral family and the sourcing-model §1/§8 history clauses staying exempt per their adjudications); and the twenty-fifth-wave additions (2026-09-14) — Check 77 extended (part (j)): the cascade's own vendor-seat/routing stragglers — the official TO's §8 SIB cadence row still routed 'Configure/buy/build routing per the capability sourcing model' (the gate the sourcing model's own §3 collapsed to two exits; the same class the wave-23/24 trues closed in the OM's §8 SIB row and §6.1 RACI row), the OM's DP team row still read 'Under the hybrid landscape' (the sibling IAP row reads 'two-tier landscape'), W3092 still carried a 'WMS Vendor' participant/step-R/lane seat against the in-suite Oracle WMS/MSCA adoption (its own Step 1 has IT configuring the dark-store WMS extension), and W1181/W1489/the financing-partner step still routed POS software changes to 'POS Vendor'/'POS Vendor Support' seats against the register's already-built in-house POS row (SSP — Store Systems & POS — owns the POS estate per sourcing model §4/§5) and the workflows' own 'per IT change management' text — all re-seated/re-pointed in place (the TO and OM rows anchor-pinned; the 'WMS Vendor' seat joining the repo-wide joined-text probe with zero legitimate live uses; the POS-vendor ban scoped to the three software-context files, the PA-37.1 terminal-hardware 'POS vendor' staying exempt as commodity procurement per the PA-138.1/automation-vendor precedent), and the twenty-sixth-wave addition (2026-09-14) — Check 24 Part C (workflows/README.md Quick-Stats table + family-subtotal reconciliation line: the folder README's own headline block — the 188/569/5,427 Quick-Stats rows, the 'Classified total' N-rows-=-U-unique-+-S-sub-workflow triple, the closing 'Family subtotal reconciliation: … = N' sentence, and the navigation row's 'all N value streams' clause — was read by no check (Check 24 Part A reconciles the family sections only; Checks 70/74 pin the root-README, bpmn/README, dmn/README, index and exec-summary surfaces), and synthetic injections proved a stale 5,426 headline row and a stale 5,426 reconciliation total both pass the full validator 0/0; the rule now re-derives every figure each run from the primary sources — VS-* directories, PA-*.md files, the PA corpus's unique ## and ### W headers, and the classification register's per-tier rows via the register_heading_hits attribution — and asserts the reconciliation line as the exact family-ordered equation of Part A's own row sums), and the thirtieth-wave additions (2026-09-15) — Check 18's register-title mirror (all 5,450 register W-title cells must equal the PA ##/### W-header canon exactly, except the two v7.31-adjudicated W423/W425 treasury-companion annotated forms pinned by literal; added after 49 stranded register titles were found on the surface no rule read — incl. the NCSD/I-SEAL/PAS 39/Intercharge token errors the semantic batches repaired in the PA files but never re-pointed into the register — and a drifted or reverted title passed the full validator 0/0 pre-arming), and Check 71's third generated artifact (the shipped role-coverage matrix is byte-verified every run via generate-role-coverage.py --check, whose only harness had been the generator's own mode — the validator never invoked it, so a PA RACI edit regenerating nothing could strand the matrix stale), and the validator's own crash class closed (the error-branch '^BAD\|' grep pipelines ran under set -euo pipefail, so any check failing with zero BAD-prefixed lines — the new rc-arm-only case — killed the run mid-report before the remaining checks and the summary; all thirty pipelines now guarded \|\| true), and the thirty-first-wave addition (2026-09-15) — Check 71's role-vocabulary census pin (generate-role-coverage.py --census re-derives the owner-resolution population every run and the numbers are pinned as the adjudicated baseline, so a batch that edits owners, aliases or the §5.3 register and regenerates the matrix consistently still cannot move the resolution population silently; the TO §5.3 status note's overclaim re-worded to the real contract in the same wave), and the thirty-second-wave addition (2026-09-15) — the role-vocabulary reconciliation (by direction, every workflow role mention must be consistent with the org of record: 495 alias mappings in generate-role-coverage.py plus 17 malformed compound-cell Owner cells rewritten in their PA files adjudicate EVERY recurring form, abbreviation and deep-tail compound to register titles, §7.2/§7.3 rosters, IT seats, department actors and governance/system/external buckets — **all 5,427 Owner cells now resolve** (owner_uncharted = 0), uncharted population 10,829 → 5,125 across participant/step prose — with the pinned census baseline re-pointed to the fully-consistent state and the generator's census mode asserting every alias target resolves to a real org actor), and the thirty-third-wave additions (2026-09-15) — Check 25's touchpoint-map title arm (all 114 §Statutory & Gap-Analysis per-VS title cells must equal the canonical value-stream-index summary-row names, population asserted; added after 64 compressed authoring-time forms were found stranded beside 50 byte-exact siblings on the surface no rule read) and Check 26's name + W-label arms (the §8.1/§8.2/§8.3 VS-name cells — 10 + 39, populations asserted — must equal the same canon, and every §1–§7 dependency-tree 'Wnnn (Label)' label must token-overlap the PA W-header canon title for that W-id or sit on the adjudicated 13-entry compressed-form allowlist; added after §2.9's statutory tax chain was found carrying 'W140 (Corporate Income Tax)' — the wrong W-id against OHS Incident Management, stranded since the initial commit while the map's own §2.9/§4 labeled it correctly — and 32 compressed §8 name forms incl. the retired VS-100 '…& IP' abbreviation were found beside 17 byte-exact siblings), and the thirty-fourth-wave addition (2026-09-15) — Check 67's extended-section arm (the 21 core-block READMEs VS-01–VS-20 + VS-73 carry Overview / Why-it-matters / Owner-&-participants / Key-dependencies / Key-controls prose no check read: every Owner line pinned to the org-of-record form, every Key-controls CTL gloss token-overlapping the register's own control objective over a joined wrapped-line read, and the repaired figure/reporting-line anchors required with the retired forms forbidden per file; added after four wrong-id control citations (CTL-229 glossed 'credit limit' in VS-11/VS-16 — the supplier-quality control — where the credit-limit control is CTL-05; CTL-337 'payroll audit' in VS-19 — the PA-32.1 returns control — where the payroll operating control is CTL-297; CTL-43 'lease/CAM audit' in VS-20 — the sales-rep credit self-approval control — where the facility/lease-cost control VS-20's own PAs cite is CTL-218), two figure conflations vs the PA canon (VS-08's total-revenue-through-terminals and VS-73's recycling split vs its own W2599/offcut per-store figures), one TO reporting-line contradiction (VS-73's Coordinator 'reports to COO' vs the Head of Sustainability / ESG line) and five drifted/mangled/phantom owner seats were found stranded on those sections), and the thirty-fifth-wave additions (2026-09-15) — Check 77 extended (part (k) + part (c)/(g) arms) and Check 59's ebs_blueprint_hits KPI arm: the EBS-maximization audit's own landscape cascade — the audit re-pointed fit-gap D13 (the Field Service dispatch core is in-suite) and E3 (timecards ride OTL) but the surfaces that ASSERT the disposition still taught the unqualified pre-audit build scope: the blueprint README §1 in-house-builds row ('installation dispatch' + the 'EBS offers no credible substitute for any of them' rationale), the executive-summary builds row, the profile §14.1 note + Store-workforce/Dispatch rows (the technician mobile device is Field Service's, not the build's), and the guide's L3 built-differentiator cell + §4.2 execution-state row — all trued in place and anchor-pinned over joined live text (the profile join strips the §14.1 blockquote's '>' markers, the ebs_blueprint_hits intro-blockquote reading), the retired pre-audit forms forbidden per document; the assumptions register's A6.2 row still carried the retired 'No strict PH data residency / Asia-Pacific hosting recommended' posture while citing technical-guidelines §2.1 (the very row the on-premises canon re-pointed) — invisible to the part-(g) per-document scope — now anchored with both retired literals forbidden in the document; and the fit-gap §7 KPI row's stranded 'current 68.4%' (the audit trued the §3 total and intro headline to 59/83 = 71.1% but missed the KPI table's 'This register, re-run per wave' row) now re-derives with the class counts every run, and the thirty-sixth-wave addition (2026-09-15) — Check 71's census baseline re-pointed to ZERO uncharted forms across all four RACI surfaces (owner/participants/step-R/step-A): the by-direction involved-role reconciliation adjudicated every previously-uncharted actor form (~3,250 mappings across title aliases, department grain and the governance/system/workforce/external buckets, with '+'-and-dash compound splitting tried after the classic ';/'-split fails and value-stream/workflow reference forms resolved as cross-department references) and the matrix regenerated to 4,201 resolved actors / 0 uncharted — any new vocabulary a future batch mints re-fires the pin, and the thirty-seventh-wave additions (2026-09-15) — Check 61's matrix R-section + gap-analysis-declaration arms (audit-matrix-refs.py extended): the matrix's own section-numbering layer joined the guard — every '## R<N>.' heading must resolve to the canonical erp-requirements.md scheme (unknown number, duplicate number and zero-token-overlap wrong-subject headings all erroring) and every section numbered R25+ must mirror the canonical section's requirement population exactly in both directions, range headings holding only in-range rows — added after the BCP section was found carrying '## R25.' against the canonical R26, the matrix's own R25 Loss-Prevention section and its own Coverage-Validation '32 sections (R1–R32)' claim (ten BCP rows mislabeled, stranded since the R25–R32 tail sections were appended to the register); and the gap-analysis totals check promoted from a frozen literal to a structural re-derivation (the '188 value streams · 569 process areas · 5,370 workflows' anchor had come to satisfy itself off a retired batch-6 note — the sixth-wave OM-Total self-satisfying-anchor class — while the live declaration had moved to 5,427; every 'Canonical totals are now' declaration must now carry 188/569 and the maximum workflow total must equal the index Grand Total, totals being add-only across batches so the max is the live declaration), and the thirty-eighth-wave additions (2026-09-16) — Check 34's owner-mirror arm (every PA-control row's Owner cell must be one of its own PA's workflow owner cells, population asserted at 569; added after CTL-623/624/625 shipped a misplaced 'CSO / VP …' owner paste against PA-131's Sustainability/ESG owner canon and CTL-735 a hyphenated 'Customer-Service Rep' against PA-168.2's canon — 565/569 rows already held the convention) and Check 71's census extended to the internal-controls-matrix Owner column as the fifth adjudicated role-vocabulary surface (80 uncharted authoring-time function-owner forms, concentrated in the gap-analysis domain families C10–C17, adjudicated via the resolver's wave-38 alias table to the real org actors owning the workflows each control row cites — every target verified to resolve to a register title, roster role, IT seat or department grain — with the census integrity assertions extended to the new table and the pin re-pointed to ctl_owner_cells=808 resolved / 0 uncharted), and the thirty-ninth-wave additions (2026-09-16, by direction) — Check 71's census extended to the step-Activity & analysis-field prose as the sixth adjudicated role-vocabulary surface (a frozen candidate extractor — paren-qualifier strip, ≥1-qualifier-word role spans ending in a role head noun, noise-prefix and documented non-role-span filters, Store-instance label normalization — sweeps every Activity cell and the eight analysis fields each run, resolving every candidate through the same owner_resolves semantics as the RACI cells): 46 uncharted prose instances across 25 forms adjudicated — plural/prefix/abbreviation variants ('AR Clerks', 'BuildRight Sales Associates', 'Marketing Ops Manager', 'CX Representative', 'QA Associate', …) aliased via a wave-39 table to the chartered titles their own workflow's Role (R)/(A) cells carry; external licensed practitioners and government officials (the PRC-licensed engineer/plumber/electrician signatory family, BIR revenue officers, LGU/DAR/NCIP/DTI officials, the accredited MRO and environmental auditor, the contractor's site engineer, the union president) bucketed external; the W695 emergency-response appointments, W484 crisis-recovery-team leads and W1177 master-data stewardship assignments bucketed governance; and 19 PA files' prose re-pointed to the org of record (bare 'X Specialist' mentions wrapped to the chartered 'Sales Associate (X Specialist)' roster form, the unchartered 'Duty Manager'/'Lead Cashiers'/'HQ Director'/'Auditee Manager'/'Legal Operations Officer' seats re-pointed to the chartered Assistant Store Managers, Department Heads, Auditee and Legal Counsel, 'Installation Service Manager' to the W600 owner canon 'Installation Services Coordinator', 'IT Administrator' to 'IT ERP Administrator', the LGU planning-officer submission re-pointed to the office) — with the pin extended to prose_role_uncharted=0 prose_role_forms=0), and the fortieth-wave additions (2026-09-16, seventh pass) — Check 46's retired-generic-vehicle literal arm (47 adopted-vehicle-annotated forms — the superseded generic 'audit management system / GRC module / facility maintenance module / lease management module / contract management system / warranty module / rebate module / expense modules / bank reconciliation module / ATP module / benefits-performance-recruitment module' row vocabulary — scoped to the workflow corpus, the doctrine documents' own cascade quotes exempt), and the forty-first-wave addition (2026-09-16) — audit-model-docs' ebs_blueprint_hits join-hygiene arm: the fit-gap register's appended pass clauses must be sentence- or semicolon-separated (the seventh pass shipped eight '…The 2026-09-16' unseparated clause runs, a ').;' doubled intro join and the lowercase 'pa-98.2' file reference; all repaired in place — the joined-text probe, the ').;' join and the lowercase reference retired per document, the corrected 'PA-98.2 ST row names it' anchor required), and the forty-second-wave additions (2026-09-16, eighth pass) — Check 46's retired-generic-vehicle literal list extended with the eighth-pass forms (the 'Inventory/ATP system' availability rows retired to GOP/INV per fit-gap D15, the 'demand planning module' rows to Demantra per C5, the four 'Transportation Management System (TMS)' rows to in-suite Shipping/Transportation Execution per C14, the generic quality-system rows to Oracle Quality per C12, the vehicle-sense 'LMS' rows to Oracle Learning Management per E2 — with W5525's vendor-SaaS posture vocabulary retired against the resolution-5 'deeper function is built — never bought' canon, the 'Lms Vendor Account Manager' matrix seat retired and the 'per W74 (learning and development)' mis-pointed L&D cross-reference form retired — plus the corrected-form provenance named per literal), and the forty-third-wave additions (2026-09-16) — Check 43 gains the Automation/Pain bullet paren-balance arm (four automation bullets quoted their steps truncated mid-paren, one pain bullet carried a corrupted generator tail) and the model-company block paren-balance arm (the workflow-gap-analysis intro's pass-list parenthetical was unclosed since initial issue), and Check 72 gains the footer-block paren-completion arm (21 unclosed Prior-segment openers across this README ×6 / the requirement-workflow-matrix ×5 / the workflow-format-guide ×10, repaired in place) with its TOTALS print moved below all appends — the count-blind placement had let a synthetic CHANGELOG letter-collision pass 0/0, retiring the twenty-ninth wave's letter arm to toothless), and the forty-fourth-wave additions (2026-09-16) — Check 21's retired-defect arms (the residual vacuous/corrupted automation-bullet classes the fragment metric could not see: the generic placeholder pair 'auto-integration; real-time data sync' / 'auto-report generation and distribution' / 'workflow automation; rule-based routing' — 28 bullets across 14 workflows whose entire Automation Opportunity content was boilerplate against the format guide's own 'vacuous' bar, invisible to the fragment regex because they carry no paren — plus the corrupted W907 auto-verb join, the empty W1210 section, the hyphenated-verb W3834 fragment that 'auto-\w+ (' missed, and trailing whitespace on automation-section lines; all repaired to the step-quoting house form with quotes verified as verbatim step substrings, and the legacy fragment metric promoted WARN → ERROR with the 2026-06-28 backlog long closed), and the forty-fifth-wave additions (2026-09-16) — Check 46's vehicle-literal arm extended with the eighth pass's dodge-forms (the demand-planning literal made case-insensitive and a supply-planning sibling added — C5; the bare-TMS token with its OTE/Route-Optimization-Engine split annotation — C14; the bare/compound quality forms with the (VS-NN) genre lookahead — C12; bare warranty — D16; lowercase vendor-rebate — B10; the revenue-module rows with the yield/ lookbehind — A11; the ERP/equipment-maintenance forms with the (EAM)-gloss exemption, the W117-pointed agreement/lease row and the calibration-management form — F5/F3; the bare-LMS enumerations incl. the WMS/LMS Labor-Management sense — E2/C6), the arm catching its own author post-extension (five further training/LMS genre rows in PA-133.2/PA-134.1/PA-171.2/PA-175.1 the corpus sweep's filtered grep had missed, trued to OLA), and audit-model-docs' join-hygiene arm widened from lowercase-letter to letter-or-digit joins (the eighth pass's F3/F5 appends 'A12 The'/'C14 The' had dodged the lowercase arm; repaired in place, teeth verified by four synthetic injections each firing at its exact rule with the clean tree silent and fixtures restored sha256-verified), and the forty-sixth-wave additions (2026-09-16) — Check 43 extended (part B gains Controls-bullet quote parity, part D gains Automation/Pain quote parity + the general 'auto-<verb>of' missing-space arm + the mid-phrase quote-cut arm): analysis bullets are quote-balanced per line with code spans masked — two Automation bullets carried a stray unbalanced close-quote after their quoted step fragment (the PA-03.1 '"Approved Factory" status in ERP"' / PA-22.1 '"Approved Promo Price" … 20 SKUs"' forms), two Controls bullets quoted their source disclaimer without ever closing it (the PA-09.2 aftermarket-battery / rebar-disclaimer forms — completed to the full source text, moving the regenerated bpmn tree at exactly PA-09.2's annotations), two Automation fragments were cut mid-phrase before the fragment's own noun without the generator's '…' marker (both 'across the 5"' — the step texts continue 'legal entities'; completed), and thirteen sibling 'auto-<verb>of' missing-space forms the forty-third wave's countof-only true had left across 11 files (auto-logof ×6, auto-fileof ×2, and one each of create/distribute/record/schedule/validate) — trued to the 'auto-<verb> of' house form; teeth verified by four synthetic injections each firing at its exact rule (stray close → odd-quote arm, re-minted logof → missing-space arm, re-cut 'across the 5"' → mid-phrase arm, truncated Controls disclaimer → odd-quote arm) with the clean tree silent and fixtures restored sha256-verified), and the forty-seventh-wave addition (2026-09-16) — Check 71 gains the shipped-tree byte-currency arms: generate-bpmn.py and generate-dmn.py gain --check modes (full in-memory re-derivation of every file, byte-compared against the shipped tree, never writing; missing files, hand-edited files, partial regenerations and stale extras all reported per file), the validator invokes both every run, and the ok gate requires both clean — closing the surfaces the seventeenth-wave content mirror never read (BPMN task names, lane sets and sequence-flow wiring, plus the entire dmn/ content layer, which until this wave carried no shipped-currency pin at all — structural validation reads shape, not text) after the forty-sixth wave's own hand-regeneration of PA-09.2 demonstrated the class live; and the 2026-09-17 additions (forty-ninth wave) — Check 21 extended with the Controls-splice arms (f)–(i): the sibling corruption class to the forty-fourth wave's Automation arms, living in the Controls `operational:` bullets the Automation loop never read — 99 bullets across 67 PA files carrying dangling empty tails ('; .', incl. one PA-190.3 line with the CTL-802 gloss spliced mid-bullet), duplicated items with the item's leading modifier dropped (the 'mandatory X; operational: X' family), items truncated on a bare connector (the mint pass split item lists on sentence periods, so 'realized vs. offered' ended at 'vs' — seven truncations), and pain-point risk clauses / mitigation-clause substrings minted as control items ('are stranded', 'but often deprioritized', 'and bank portal access, creating a fraud…', 'for stock associates' — the splice signature), all repaired to items grounded in each workflow's own Pain Points/Steps by fix-controls-splice-artifacts.py (50 mechanical exact-match repairs across 43 files) + fix-controls-splice-artifacts-2-context.py (49 context-grounded repairs across 29 files), the whole class retired ERROR-armed with the clean tree silent and the teeth verified by synthetic re-mints firing at their exact arms through the full validator; the 2026-09-17 fifty-first-wave review closed the last three no-check-read surfaces — Check 16's final-newline arm (two PA files shipped without the trailing newline every other tracked file carries, readable by no check: the shape arm greps the last non-empty line, the generators parse structure, never the file tail), Check 23's extension to every non-PA .md's intra-file '#anchor' links (the 27 non-PA anchor links were enumerable by no check — Check 39(d) resolves only cross-file '.md#' anchors, Check 66 drops fragments; the sweep found the class clean, the sole unresolved intra-file anchor repo-wide being the CHANGELOG's own literal W-anchor prose example ('[W…]' linked to '(#…)', exempt per Check 66's convention)), and Check 43 part G (paren/bold/quote balance per paragraph block across 02-oracle-ebs/ + 07-methodology/ — the two doctrine/methodology folders sat outside the C/E glob and outside Check 72's footer scope; fenced code stripped, code spans masked, table rows row-scoped, footer/history blocks left to Check 72's adjudicated masking; the sweep found the folders clean, the only imbalances the adjudicated quoted-citation forms '§4 resolution 32))' / ').;' / '(vN.M' inside footers and the methodology-index Contents rows), and Check 46's vehicle-literal arm extended by the 2026-09-17 fifty-second-wave plain-wording sweep (+68 retired forms — the same superseded vehicles in wordings the exact-literal guards could not see: maintenance/facility/PM work-order, CAPA, quality-inspection, demand-forecasting, treasury, intercompany, contract, master-data, lease, applicant-tracking, markdown, margin, dedup, training, succession, ERP-payroll, ERP-loyalty, revenue-recognition, rebate, compliance-case, warranty, construction-RFP, yard, route/load-planning, fleet and registration-expiry-alert forms, each annotated with its adopted vehicle and verified at zero pre-arm), and the 2026-09-17 fifty-third-wave review brought the 02-oracle-ebs licensing BOM under audit-model-docs' guard with the licensing_bom_hits arithmetic rule (every scenario total re-derived from the BOM's own line items — the doc had shipped with the fifty-second-wave session untracked, unmapped and unintegrated; now document-map-rowed, tree-listed, and true to the register's E5–E8 payroll-build range), and trued the wave's own stale self-descriptions (fit-gap §4 resolution 34's trailing cells, the two fix-ebs-vehicle-stragglers Contents rows and the root-README tree rows quoted a mid-wave snapshot — '99 across 30 / +49 / three steps-table files' — against shipped artifacts carrying 132 exact-match repairs across 61 PA files (109 script + 23 inline), +68 literals and nine regenerated bpmn files); and the 2026-09-17 fifty-fourth-wave review extended the guard with the licensing_bom_hits footing arm (§1's ecommerce driver must keep stating the derived ~4.5-lines/order basis its EOL/Pooled-line pricing rides — no lines-per-order canon exists anywhere; §2's Financials and MSCA user build-up notes must carry their explicit 'rounded to' footing clauses, the MSCA DC figure trued to §1's own 600-user driver — anchors and retired forms checked against the footer-stripped body after the arm's first draft satisfied its anchors off its own change-note, caught by the injection teeth) and Check 75's portability arm (no 07-methodology tool may resolve its repo root by a hardcoded '/home/<user>/erpplans_ebs' path — the eighteenth-wave shipped-tooling class the fifty-second-wave straggler scripts had re-introduced; both re-pointed to repo-relative resolution and re-run clean at APPLIED 0 with an untouched tree); and the subsequent 2026-09-17 EBS-instance verification run executed the documentation set's verification against the designated Oracle EBS 12.2.12 Vision instance and shipped 02-oracle-ebs/ebs-vision-verification.md under audit-model-docs' guard with the vision_verification_hits rule (39 FP footprint rows + the VT transactional table re-pinned 5 → 10 rows by verification round 2, the §2 verdict tally re-derived from the FP table's own CONFIRMED cells, the VF-1..VF-11 findings register and the instance-evidence anchors pinned every run — findings VF-1 RM&I naming correction, VF-2 ICM/AMW obsolete-not-installed retirement chartered, VF-3 Site Hub not present, VF-4 GOP standalone annotation, VF-5 E2 naming nuance, VF-6 instance follow-ups; round 3: eight of the ten paths PASS end-to-end, VT-2 GL and VT-7 receiving root-caused to conclusive SR-grade instance defects); and the 2026-09-18 fifty-fifth-wave review swept the actual-org gap-fill's own live-surface stragglers (the TO §1 design stance still at HQ 511 under a re-pointed states table; the classification register's live W10 cell still at 6,911 employees; the licensing-BOM §1 driver breakdown still '+ 511 HQ' under its 6,932 leading figure), trued the guard's own stale two-state error-message anchors (511/6,911 → 532/6,932), and brought the cross-repo quote-coverage review under audit-model-docs' DOCS set with quote_coverage_hits (G1–G17 / D-1–D-9 register skeletons, the 7,247-vs-6,932 canon row, the v1.1 post-review supersession note), licensing_bom_hits with the driver-row footing arm, and gap_fill_straggler_hits (the classification live-line 6,911 ban + the TO §1 stance anchor); and the 2026-09-18 step-level W-reference resolution — Check 78 step-level W-reference resolution (fifty-seventh wave, 2026-09-18: every dotted W<id>.<step> citation resolved against the parent workflow's own steps table under the generator's canonical step model, letter forms row-first then sub-item, range endpoints per-endpoint — the family's 17 defect instances repaired: six W-keyed W37 step rows renumbered to bare 11–16 (+6 bpmn tasks, +1 dmn decision/+4 rules), the W12.3/W29.1–29.3/W29 mis-namespace PA cites trued to VS-12.3/VS-29.1–29.3/VS-29, the phantom W285.6a and W36.12 cites trued to W285.6 and the blessed W110.5/W36 pair, the W2.5–6 PO-approval cite trued to W2A.5–6, and the salvage Time Estimate's W12.2 backroom-cleanup cite trued to W743); and the 2026-09-21 fifty-eighth-wave builder-attribution arm — the round-two reconciliation (2026-09-18 (i)) declared the Check-78 comment + ok-message re-pointed fifty-fifth → fifty-seventh but shipped it only as an uncommitted working-tree edit, so Check 40 pins the reconciled fifty-seventh-wave review form on both declared surfaces and bans the retired form anywhere inside the Check-78 block; and the 2026-09-21 oratest capability verification — the CitiHardware R12.2.4 PROD-clone rig (~/access_oratest: .211 proddb / .212 cloneapp, BG 81 CITIHARDWARE INC, 18 PHP ledgers) joins the documentation set as the second installation of record and 02-oracle-ebs/ebs-oratest-verification.md joins the DOCS set (20 docs) with the oratest_hits structural rule (section skeleton; the 39-row FP × rig re-map and 10-row VT × rig matrix re-pinned; the §3 rig tally re-derived from the VT rows' own verdict cells — 9 GREEN + 1 PARTIAL, with both Vision instance-side blocks VT-2 GL and VT-7 receiving GREEN on the rig and VT-9 HR hire / VT-10 FA mass additions executed end-to-end for the report; the OR-1..OR-6 findings register; the rig-evidence anchors release 12.2.4 / employee 10029015 / asset E2E-ERP-F-001 / posting request 76976818 pinned every run) |
| [generate-bpmn.py](generate-bpmn.py) | BPMN 2.0 generator (2026-09-05) — converts every confirmed workflow block in `01-model-company/workflows` into the validated `bpmn/` tree (569 files / 5,456 processes with lanes, wiring and full DI); idempotent byte-identical regeneration; byte-verified every run by validate-repo.sh Check 71 — structurally since the seventh wave, via the documentation/start/annotation content mirror since the seventeenth, and via the generator's own `--check` mode since the 2026-09-16 forty-seventh-wave review (full-tree in-memory re-derivation and byte-compare: task names, lane names, wiring and every remaining surface the partial mirror never read) |
| [generate-dmn.py](generate-dmn.py) | DMN 1.3 generator (2026-09-05) — extracts rule-shaped markdown tables and tiered PHP authorization ladders from the workflow corpus into the validated `dmn/` tree (41 files / 80 decisions / 343 rules; 197 ambiguous rule sets deferred rather than faked); idempotent byte-identical regeneration; byte-verified every run by validate-repo.sh Check 71 — structurally since the seventh wave and via the generator's own `--check` mode since the 2026-09-16 forty-seventh-wave review (full-tree in-memory re-derivation and byte-compare; the dmn/ content layer had carried no shipped-currency pin at all before it) |
| [generate-role-coverage.py](generate-role-coverage.py) | Role–workflow coverage generator (2026-09-14) — parses every PA file's Owner / Participants / Steps-table Role (R) / Role (A) fields, the criticality register's confirmed tiers and the official TO (§5.3 register, §7.2 store roster, §7.3 DC roster) into `01-model-company/role-coverage-matrix.md`: per-role workflows-owned / participated / step-R / step-A / tier-mix across the 5,433-workflow corpus, with resolution order §5.3 register → field rosters → IT product-model seats → department-level actors → system/governance/workforce/external → an honest uncharted watchlist; `--check` byte-verifies the shipped artifact; byte-verified every run by validate-repo.sh Check 71 since the 2026-09-15 thirtieth-wave review, with `--census` re-deriving the owner-resolution baseline pinned there since the thirty-first wave and carrying the thirty-second-wave reconciliation (296 adjudicated alias mappings; the census asserts every alias target resolves to a real org actor) and the thirty-sixth-wave full-RACI-surface reconciliation (by direction, every involved role must be consistent with the org: ~3,250 further adjudicated mappings across title aliases, department grain and the governance/system/workforce/external buckets, '+'-and-dash compound splitting tried after the classic ';/'-split fails, and value-stream/workflow reference forms resolved as cross-department references — the census now reads uncharted_forms=0 over all four surfaces, pinned; the thirty-eighth-wave reconciliation (2026-09-16) extended the census to the internal-controls-matrix Owner column as a fifth adjudicated surface — 80 authoring-time function-owner forms mapped via a dedicated alias table to the org actors owning the workflows each control row cites, census fields ctl_owner_cells/ctl_owner_resolved/ctl_owner_uncharted pinned by Check 71; the thirty-ninth-wave reconciliation (2026-09-16) extended the census to the step-Activity & analysis-field prose (the sixth surface, frozen candidate extractor, 46 instances/25 forms adjudicated via the wave-39 alias table + external/governance buckets + 19 PA prose re-points), fields prose_role_uncharted/prose_role_forms pinned at zero) |
| [classify-workflows.py](classify-workflows.py) | Keyword-driven criticality classifier — regenerates `01-model-company/workflows/workflow-criticality-proposed.md` (Tier 1/2/3 proposal for every workflow not in the hand-confirmed register) |
| [confirm-all-workflows.py](confirm-all-workflows.py) | One-time full-coverage confirmation pass — promotes every remaining keyword-proposed workflow into the confirmed register using the calibrated batch rules (statutory execution → T1, analytics → T3, operational support → T2), appends the family-grouped confirmation section, reconciles the register Summary/intro/tier counts, regenerates the (now empty) proposed register, and updates downstream figure spots. Closed the unclassified backlog 2,596 → 0 on 2026-06-28 |
| [confirm-postcatalog-14.py](confirm-postcatalog-14.py) | One-time post-catalog confirmation pass (2026-09-02) — confirms the fourteen post-catalog workflows W5497–W5510 with recorded per-workflow adjudications under the same calibrated rules (adoptions, three statutory-execution promotions W5498/W5503/W5504, one analytics promotion W5501, demotions W5509 → T3 and W5507/W5510 → T2), appends the per-tier Post-Catalog Confirmation subsections + dated note + v7.42 footer to the register, reconciles Summary/intro/tier/coverage counts (5,372 → 5,386 rows; 5,349 → 5,363 unique; T1 1,381 / T2 3,249 / T3 756), regenerates the proposal register empty via classify-workflows.py, and re-points every downstream figure spot (workflows/README Quick Stats + nav row, root-README coverage rows + tree rows, format-guide anchors, dependency-map intro + v4.16 footer, gap-analysis current-state clause). Every replacement asserted exactly-once; not idempotent by design |
| [backfill-controls.py](backfill-controls.py) | `### Controls` blank-line normalizer + CTL-XX backfiller — reverses `internal-controls-matrix.md` into a workflow→control map and injects real CTL-XX references into Controls sections that lack them; also restores the missing blank line before the next `###` header |
| [add-pa-controls.py](add-pa-controls.py) | One-time register closure — adds C26–C33 Process-Area Operating Controls (CTL-240–CTL-808), one derived control per process area mapped to every workflow of that PA (objective/type/owner/activity derived from the PA's own workflow steps; honest-draft). Together with `backfill-controls.py` this took CTL-XX citation coverage to 100% of workflows (validator Check 21 target met) |
| [add-expansion-anchors.py](add-expansion-anchors.py) | One-time register extension — adds the C18–C25 "Expansion & Legacy-Block Domain Controls" (CTL-172–CTL-239, one anchor per previously-uncovered value stream across Core/Expansion/Statutory blocks), extending `internal-controls-matrix.md` to full value-stream coverage (239 controls); verifies every W-ref resolves and every Req Ref exists before writing |
| [backfill-participants.py](backfill-participants.py) | `\| **Participants** \|` row backfiller — derives the missing Participants field for each workflow from the roles named in its own Steps table's Role (R)/(A) columns (owner and system actors excluded, abbreviation variants normalized). Honest-draft principle as `backfill-time-estimate.py`; closed the 224-instance gap in VS-53–VS-63/86/87/99/189 on 2026-06-27. Idempotent; companion to validator Check 22 |
| [add-automation-controls.py](add-automation-controls.py) | Automation/Controls field generator — inserts workflow-specific Automation Opportunity and Controls sections derived from steps/touchpoints. Now CTL-map-aware and emits complete sentences (draft quality, refined per-workflow); companion to `backfill-controls.py` for retroactive CTL backfill |
| [defragment-automation.py](defragment-automation.py) | One-time repair of legacy mid-phrase fragment Automation bullets — regenerates any section containing a generator-fragment bullet into complete `- System {action} of "{step}" (replaces manual Step N).` sentences. Idempotent; verified to destroy no hand-written content |
| [backfill-time-estimate.py](backfill-time-estimate.py) | Mechanically derives the `### Time Estimate` section where absent — parses each workflow's per-step Durations into a draft roll-up, explicitly labelled as pending human annualization. Honest-draft principle (values come from authored steps; no fabricated throughput math). Its 407 remaining drafts were finalized by `finalize-time-estimates.py` on 2026-08-28 |
| [audit-semantic-anchors.py](audit-semantic-anchors.py) | Semantic-sample anchor guard (2026-08-29, consistency review #44; quote-integrity rules added by review #50) — retires the literals repaired by the stratified semantic samples, enforces register-equal Participants counts for the section 13.1 merchandising roles, and verifies every step-quoting automation bullet quotes its referenced step verbatim at a word boundary (containment + mid-word-clip rules; 884 clips and 50 '-logy' mis-repairs swept 2026-08-29, 12 noun-phrase bullets allowlisted). Companion to validator Check 62 |
| [audit-oracle-conformance.py](audit-oracle-conformance.py) | Oracle EBS best-practice conformance guard (2026-09-23, issued with the custody-and-treatment review; extended same day with the corpus-wide second pass, canon §7) — enforces the `02-oracle-ebs/oracle-ebs-conformance-standards.md` behavioral canon over the workflow corpus: anchor rules requiring the conformance clauses at their repaired cells — the W1690 custodian-of-record assignment and Assigned-To field, the mass-additions queue gate, the W1690 tractable-item pool, the W1692 custody-acceptance scan, the W1693 release-and-accept transfer chain, the W1698 corporate-book/derived-tax-book language, the W1706 custodian count attestation, the W1828 CIP custody handover, and the W1670/W1673 controlled-issue clauses, plus the §7 second-pass cells — the W1695 eAM vehicle naming (PM scheduling, work-order recording, work-order history) with the coverage-map eAM row's VS-35 estate anchor, and the W3234/W3235/W3238 IT-asset custody chain (mass-additions routing, the named-individual Assigned-To custodian, the tag-scan custody acceptance, the deployment release-and-accept with its Assigned-To update, and the spare-pool re-custody), plus the §8 third-pass cells — the W89/W9A bank-reconciliation independence seating (the entity GL Accountant, independent of payment execution, OC-24, with the CTL-19 owner anchor) and the W9A step-17 close-sequence clause (sub-ledgers closed and reconciled before GL locks, OC-25), plus the §9 fourth-pass cells — the W677 GL-budget-organization commitment accounting, blanket-PO release and AME routing, the W83 budget-gate and funds-checked requisition clauses, the W833 ERES sign-off with submitter/approver separation, the W286 OM/AR AutoInvoice + AR Revenue Management billing clauses, the PA-39.2 OTM co-op chain and the W4201/W4207 booth custody chain, plus the §10 fifth-pass cells — the W10 step-9 payroll journal-import chain (unposted batches under the Payroll PH source, OC-26, also pinned on W1416 step 1, W1384 step 5, the PA-19.5 resignation posting and the coverage-map HR & Payroll row), the W644/W1416 no-parallel-accrual and W638-routed-correction clauses (OC-27) and the Treasury-transmission clauses on the W641/W643/W644/W1416 payment rails (OC-28) with the W1306 volume anchor — retired-form bans on the replaced phrasings, and informational corpus scans (supplies capitalization language inside PA-34, register-owner-on-a-receiving-step SoD inside PA-35.1, unattested counts, the corpus-wide register-owner SoD census over all 569 PA files with the receivable/cash-received/receiving-entity financial senses exempt, the bank-reconciliation census scan with PA-18.1's daily-visibility steps and the adjudicated channel-reconciliation/fee-monitoring operations senses exempt, and the payroll payment-file transmission census scan (a payroll/HR-responsible step transmitting or releasing a bank file with no Treasury seat reports for triage — OC-28)); `--guard` exits 1 on any anchor or retired-form hit per the house convention |
| [final-semantic-coverage.py](final-semantic-coverage.py) | Final full-coverage semantic pass (2026-09-02, consistency review #71) — extends the Check 62 sampling loop from the 2,241 stratified full-read workflows to all 5,363 via a detector suite over the 3,122 unaudited W-blocks (D1 same-row per-store-rate vs chain-total coherence with kg/ton unit awareness; D2 statutory-citation adjudication against the audited-corpus baseline + the web-verified canon list; D3 same-line hours/week vs hours/month contradiction; D4 chain-/day cadence suspicion), with every flagged spot read and adjudicated by hand — three citation defects repaired (W998's retired 'DENR AO 2005-10' septic-tank attribution → PD 856 Ch. XVII/NPCP; W4827's truncated 'D.O. 13' → D.O. 13-98 with the title/TOC/anchor/register/README cascade; W2148's 'RA 9194 AMLA' → the principal act RA 9160 per the VS-86 canon), 16 citations added to the canon list after verification, and 11 false-positive constructions allowlisted with documented reasons. Honest labelling: detector coverage + targeted reads, not 30×104 full reads. The 2026-09-23 seventy-fourth-wave review admitted the 62 post-closure workflows W5518–W5579 (gap-fill batches 24–29, shipped after the closure without the transition path) in one pass — full read of every block, D1–D4 over the unaudited set (0 non-citation flags), the five D2 citation hits adjudicated legitimate in context (RA 10175 PNP-ACG referrals W5558/W5559; RA 7279 UDHA W5560; RA 10821 duty-of-care framing W5536) and added to KNOWN_GOOD, four in-read defects repaired the same pass (W5529's W336 misdirected balance-sheet-workspace citation → W636; W5578's W327 misdirected fleet-records citation → W198; the '~99,000 customers' population mislabel against the W330 people canon on W5536/W5555/the classification register; W5522's doubled fit-gap clause), the registry brought to 5,432/5,432 through W5579 (W5580 — batch 30 — rides the documented transition path for its semantic read; the registry closes at 5,433/5,433), and the W634 component-sum false positive added to the allowlist — both modes exit 0 |
| [draw-batch26.py](draw-batch26.py) — [draw-batch27.py](draw-batch27.py) — [draw-batch28.py](draw-batch28.py) — [draw-batch29.py](draw-batch29.py) | Fork-local semantic-audit sample drawers (consistency reviews #72–#75, batches 26–29; seeds 7070/7171/7272/7373) — the stratified draws mirroring the prior batches: 104 workflows, 13 per value-stream family, expansion-weighted within family, excluding all previously audited W-ids via `semantic-audit-coverage.txt`. The fork-local line's batches 22–29 read 2,241 → 3,073 W-ids in full before the branch reconciliation merge folded the lineage into the closed 5,363-id registry |
| [audit-misdirected-ctl.py](audit-misdirected-ctl.py) | Misdirected-CTL-citation family measurer (batch-26/27) — measures PA files whose every workflow block cites one foreign core control with fabricated glosses (the expansion-era paste family), and the isolated colon-form citations; output feeds the re-map worklists. `--guard` armed by the 2026-09-10 eighteenth consistency review (which also re-pointed the script — and its nine sibling tools — off a hardcoded `/home/riddler` absolute path that had made it unrunnable outside the original author's machine): zero paren-form citations naming a different PA (the unambiguous wrong-PA defect class) and the batch-13-adjudicated 170-citation colon-form census (total, per-PA and per-cited-CTL distributions) re-derived and pinned every run, so any new paste, re-map or deletion fires and forces a conscious re-adjudication with the baseline re-pointed (the Check-74 deferred-anchor pattern) |
| [classify-isolated-ctl.py](classify-isolated-ctl.py) | Isolated colon-form CTL-citation classifier (batch-27 worklist item B) — scores each `- CTL-NNN: <gloss>` line's gloss against the cited control's real matrix description (content-token overlap); high overlap = deliberate cross-reference, low = paste/fabrication candidate |
| [fix-ctl-paste-families.py](fix-ctl-paste-families.py) | One-time re-mapper of the 36 paste-family misdirected CTL citations (batch-26, review #72) — re-points each affected block's Controls to the file's own PA control, preserving the operational clauses |
| [fix-ghost-roles-batch26.py](fix-ghost-roles-batch26.py) | Ghost-role adjudication sweep for the batch-26 families (review #72) — unifies uncharted executive-title variants on charted/majority owners per profile §11.1/§3.3/§13.1, extending the batch-24 precedent. Sunset-guarded by the 2026-09-10 eighteenth consistency review: the sweep predates the official table of organization of record (v2.0/v2.1), which deliberately charts 'VP Finance & Accounting' and 'VP HR' as sub-CFO / sub-CHRO seats, so the script now refuses to run while those seats are charted (re-running it in the eighteenth wave's portability sandbox was found to rewrite the official §7.1 tree) and its summary print's latent AttributeError was repaired |
| [fix-ghost-titles-batch14.py](fix-ghost-titles-batch14.py) | Ghost-title charter sweep (batch 14) closing worklist item A — maps 25 uncharted executive/director title families to TO-charted seats (~1,090 spots), each target passing the two-sided test (charted seat + remit coverage), per the adopted target-state TO and the corpus's majority forms (the batch-24/batch-26 precedent). Sunset-guarded by the 2026-09-10 eighteenth consistency review: the family is closed and the sweep's own batch-14 record note in workflow-gap-analysis.md matches these rules, so the script refuses to run while that note exists (a re-run rewrites the frozen gap-history record, sandbox-verified) |
| [fix-pa211-order.py](fix-pa211-order.py) | One-time PA-21.1 scrambled-section-order canonicalizer (batch-27, review #73) — reorders 11 generation-era blocks to the corpus convention (header → Steps → System Touchpoints → Pain Points → Automation → Controls → Staffing → Time Estimate) with the section-set preservation asserted |
| [audit-matrix-refs.py](audit-matrix-refs.py) | Matrix ghost-row and summary-doc anchor guard (2026-08-29, consistency review #43; extended by the 2026-09-15 thirty-seventh-wave review) — errors on requirement rows mapped only to non-header W aliases, matrix '## R<N>.' headings that do not resolve to the canonical erp-requirements.md scheme (unknown number, duplicate number, or a title sharing no significant token with the canonical section title) or R25+ sections whose requirement population does not mirror the canonical section exactly (range headings must hold only in-range rows), gap-analysis 'Canonical totals are now' declarations whose VS/PA figures are non-canonical or whose maximum workflow total (totals are add-only across batches, so the max is the live declaration) has fallen behind the value-stream-index Grand Total, or missing verified anchors in technical-guidelines.md. Companion to validator Check 61 |
| [audit-exec-ctl.py](audit-exec-ctl.py) | Executive-summary anchor and CTL citation-scope guard (2026-08-29, consistency review #42) — canonical anchor figures present, retired totals absent, and spend-control citations spend-scoped. Companion to validator Check 60 |
| [audit-model-docs.py](audit-model-docs.py) | Root-level model-company document integrity guard (2026-08-29, consistency review #41; extended 2026-09-02 by review #68; extended 2026-09-03 with a methodology-index rule — every '(vN.M' version pin and the two-state TO-anchor description in this index must equal the pinned doc's own Document Version footer / live guard anchors; extended 2026-09-04 to also cover the AI-first operating guide, verified clean on dry-run, with a guide_figure_hits structural rule re-deriving its catalog triple, Tier-ladder sentence and control/requirement canon-table citations from the primary registers) — token resolution against the live registers, doc-scoped §-ref resolution, and retired-figure literals for mobile-app-strategy.md, data-migration-mapping.md, assumptions-and-design-decisions.md and — since the 2026-09-07 eighth-wave review — data-volumes-and-integrations.md (9 docs, with the dv_volume_hits §1.1/§1.2 arithmetic rule, the exec_tree_hits repository-tree pin, and the reality_check_hits ~300/day AP anchor); review #68 brought the two organizational documents (optimal-table-of-organization.md, it-product-operating-model.md) under the same guard after finding three figure/citation defects between them, added doc-scoped retired literals + required corrected anchors, and a structural §7.3 DC-roster group-sum rule; the 2026-09-07 eighth-wave review brought data-volumes-and-integrations.md under the guard (now 9 docs — it had shipped with zero validator coverage), admitting the ###/#### sub-workflow ids into the token register (W7C), adding a doc-scoped 6,715 exemption (there the canonical AP merchandise-invoice count, not the retired headcount total), and adding three structural rules: dv_volume_hits (§1.1 peak cells re-derived as daily × declared factor, no factor-less peaks, §1.2 size column must sum to the stated increment and the retention row to ~10× it), exec_tree_hits (exec-summary Repository-Structure tree must equal the on-disk top-level directories), and reality_check_hits (retired ~450/day AP parenthetical + corrected ~300/day anchor); the 2026-09-07 ninth-wave review repaired the reverse-direction companion-pin family no rule read (the sourcing model's header 'Companion to' OM pin stranded at v3.3 by the batch-20 cascade that re-pointed only its §13 sibling, the §13 OM row frozen at v3.4, the sourcing footer companion-pin chain broken by the sixth-wave OM v3.8 bump — sourcing bumped to v2.8 with its own re-point clause — and the OM §13 'this vN.M model' self-pin still at the 2026-09-03 v2.4) and added companion_pin_hits, pinning all four surfaces to the pinned doc's live footer every run. The tenth-wave review adjudicated the sixth wave's flagged VS-151 residual (the §4.9 SSP row's own enumeration claimed VS-151 while its 882 figure excluded it, DP's 213 included it, and the mismatch traced to the v1.0 issuance itself) in favor of the unanimous mapping side — SSP 906 / DP 189 — and the same re-derivation exposed the hand-maintained subtotal split sitting ±3 off its own member rows (4,913 + 513 declared vs 4,910 + 516 derived): subtotals re-based to 4,934 + 492, OM bumped to v3.9 with the sourcing companion chain re-pointed (sourcing v2.9) and om_reconciliation_hits extended to re-derive every §3.2 per-team cell, both subtotal cells and every §4.9 per-team workflow cell from the §4.1–4.8 mapping tables and the on-disk `##` headers each run. The 2026-09-09 thirteenth-wave review trued the official TO's v2.0 register issuance (the footer's '≈150 distinct roles' → the register's true 188 rows / 187 distinct titles; the §5.3 span note's 'Controller at 7 directs' → 7 sub-team lines / 8 register reporting rows in-band; the §5.2 GL & Consolidation mix cell conformed to its register rows; TO v2.1) and added to_register_hits — §5.1's 18-department columns and two-state totals, the three §5.2 sub-team tables, every §5.3 department foot and its §5.1 pairing, the register-total line, the quoted distinct-role count and the span-note row-count re-derived every run. The 2026-09-09 sixteenth-wave review trued three stranded-derived-figure classes — the revenue-per-employee quotient 9.22M → 9.21M (the v2.22 rebalance bumped the §4 division's denominator 6,757 → 6,762 without re-deriving the quotient; stranded on the profile §4 note, assumptions A2.6, PA-133.3's Volume row and this module's own #41 self-description), the §15.2 Master-Data trade row and the eleven stranded live trade-account citations (requirements CRM-003, the VS-11/VS-57/VS-09 PA surfaces, the VS-140/VS-154 READMEs) trued to the §9.2 canon (the v2.23 canonicalization moved §9.2/§10.3 to the 5,200/5,400 scheme but left them) — and added profile_derived_figure_hits, re-deriving the quotient and account counts from the §9.4/§4/§9.2 rows every run, cross-footing §10.3/§15.2, and sweeping the live corpus (gap-analysis scenario-table rows, CHANGELOG, generated trees and footers exempt) for disagreeing citations. The 2026-09-14 twenty-ninth-wave review extended profile_derived_figure_hits with the profile's three remaining live employee-count rows (§11.2 Payroll-Parameters 'Total Employees', §15.2 Master-Data 'Employees', the §17 user-adoption bullet) re-derived from the §4 canon every run — the structure promotion re-based §3.3/§4/§11.1 but stranded all three on the retired 6,762 — and companion_pin_hits with the mirror OM-header→sourcing-model pin (found at v3.1 against the sourcing footer's v3.2 after the same-day bump; the ninth-wave rule pinned only the sourcing→OM direction). Companion to validator Check 59; the 2026-09-16 forty-third-wave review extended ebs_blueprint_hits with the document-map paren-balance arm (the eighth pass's fit-gap-row append shipped a doubled close-paren the fit-gap-scoped join-hygiene arm could not see); the 2026-09-21 sixtieth-wave review extended licensing_bom_hits with the Fusion employee-metric arm (every §3.1/§3.2 Employee-metric quantity equal to the §1 driver canon and the §3.2 Suite Employee tier equal to canon − the professional tier, its prose footing clause re-derived — the 2026-09-18 gap-fill's literal sweep had left that one DERIVED cell at 3,311 = 6,911 − 3,600, self-consistent with its own B2 totals and so invisible to every other arm) and gap_fill_straggler_hits with the requirement-register arm (live-line 6,911 ban, the interleaved per-round version-footer lines exempt, and all seven rows the register's own v24.2 footer declares an employee/user-count family required to carry the 6,932 canon — the sweep had re-based only two of the seven); the 2026-09-21 sixty-first-wave review extended ebs_blueprint_hits with the README navigation-row arm (the root-README tree row and the blueprint README's document-map row quoting the fit-gap register must carry the re-derived 103-row / 80-standard counts with the retired 97/74 forms banned — batch 26's cascade had stranded the register's own counts on both rows); the same day's manuals-alignment sweep verified every batch-26 product claim against the shipped R12.2 PDFs and caught the coverage register's false 'No EBS POS product exists' confirmation (the library ships 122crposig/122crposug, the supplier-side Channel Rebate & POS engine — trued to the checkout sense and recorded not-needed in §5, arithmetic 164 → 165 products / 32 → 33 recorded), re-pointed W5579's BPA 'precedence' wording to the manual's rule-order concept (PA-16.2), and added ebs_doc_coverage_hits' POS-truth arm (crpos citations + Channel Rebate name + checkout anchor required on the §3 row, retired bare form banned, §5 record required) and ebs_blueprint_hits' POS-row arm (fit-gap D6 + coverage-map POS/Retail must carry 'No EBS POS checkout product', bare form banned); sixty-second wave (2026-09-21) — closed the batch-26 live-census class (retired 5,426/5,427/5,430 banned on the footer-stripped live bodies of the eight census documents, the 5,432 anchors required at the fit-gap-intro/coverage-framing/blueprint-README/AI-first/OM/tool-row cells, the TO's '5,432 of 5,432 owner cells' joined-form required, the virtual-gemba CANON_WORKFLOWS pin required at 5432) and tightened licensing_bom_hits' §2.10 chain to the exact derivation from the stated support cell (±0.5), catching the 5-yr TCO off-by-one the ×2.1 approximation tolerance had eaten; same-day capability sweep — verified the remaining adopted-vehicle mechanics against the shipped manuals (Lockbox/Deposits/Commitments, contingent-labor timecard-billed rate lines, cXML map formats, ECC data sources, Succession Planning readiness reports, GL budgetary control, Site Hub attributes, Sourcing RFQs/auctions, iSupplier ASNs, Project Billing retention, IBI/OKS warranties, Depot Repair, TeleService escalations, iRecruitment notifications) and trued two undocumented vocabulary classes (L&FM 'rental contracts/reservations' → rental-period lease contracts per 122oklug; 'issued-but-unreleased' → 122apug's 'issued but not matured'), guarded by ebs_blueprint_hits' manuals-vocabulary arm and Check 46's two new workflow-corpus literals; sixty-third wave — ebs_doc_coverage_hits re-derives every basename-cited '/ Np' from pdfinfo (combined per-product sums, 'across k files' checked; 23/23 green) and pins EDC-21 at the library-truth 2-guides/380p HRMSi citation (the retired 3-guides/474p double-count banned); batch 27 (same day) — the coverage register completed (all 25 EDC rows dispositioned, 0 open: six adoptions B14/D18–D20/F7/H18, six realization trues A10/B10/C5/C10/D14/H5, thirteen §5 records) with fit-gap re-based to 109 rows / 86 standard = 78.9% (expect-dict 44/42, total pin 109, charter-note anchor re-pointed) and generate-role-coverage.py's PROSE_NON_ROLE set gaining 'Oracle Customer Data Librarian' (the D20 product-name-ends-in-role-head class); the same day's Vision-findings re-disposition re-pointed the expect-dict 44/42 → 44/41 with BUILD 15 (85 standard = 78.0% of the unchanged 109 rows; the §7 charter-note execution anchor and the Reading-note 6-new-scope split re-pointed) and Check 46's vehicle-literal guard gained the four retired forms (Oracle Internal Controls Manager, ICM, Revenue Management and Invoicing, RM&I); and the Component-GPL true-up (same day) verified every EBS-side licensing-BOM line against the in-repo price-list PDF (applications-price-list-070574.pdf, September 10, 2026), filling the eight † part numbers and adding licensing_bom_hits' GPL arm — no 'TBD †' form on the Scenario-A side (the † rows belong to Fusion §3.1/§3.2 only), the eight part-number anchors required in their §2 line-table cell form (pipe-delimited table cells, closing pipe included), the 'no Component-GPL SKU** to the same register' clause and the five no-SKU products (Credit Management, Oracle Quality, Engineering, Project Management, EAR) required in §2.11's custom-quote register, and the arithmetic arms re-deriving the restated $50,228,665 / $11,050,306-yr / $83,379,583 / $105,480,195 totals each run; sixty-fifth wave (2026-09-22) — three derived-surface stragglers of the same day's re-disposition cascade trued and guarded: the coverage register's §6 guard-note arithmetic quote re-pointed from the pre-batch-27 baseline (107 + 33 + 25) to §2's restated 90/50/25-then-closed sentence with the §6 quote re-derived from §2 every run (ebs_doc_coverage_hits §6-quote arm), data-migration's §2 W0 sequence-tree line re-pointed from 'the control register into ICM (24)' to the Audit & GRC platform its own re-pointed row 24 names (ebs_blueprint_hits sequence-tree arm), and the BOM's two Fusion Risk Management parity tags + §3 note re-worded from the retired ICM anchor to the H11 capability naming its sibling Fusion Revenue Management tag already carried (licensing_bom_hits retired-parity-anchor arm); the 2026-09-23 Component-GPL sellable-SKU sweep added gpl_sku_sweep_hits — the coverage register's §7 price-list direction (the §7 result sentence re-derived against the price-list PDF's part-number universe, every cited part existence-checked, the four flag pins and the L72189 collision pin required, the UPK band count re-derived; teeth synthetic-injection verified at the exact arms); and the 2026-09-23 sixty-sixth-wave review added calibrated_volume_hits — the production-volume calibration's derived-surface stragglers (46 volume-citing cells across 30 documents kept the retired canons after the v4.7 §1.1/§1.2 rows moved: ~5,000 replenishment orders/month vs the ~50,000/month production canon with physical deliveries deliberately unchanged at 2–3 consolidated drops/store/week, ~18,000 PO lines at ~15 lines/PO vs ~12,000 at ~7, ~100 GB/year and ~1,000 GB storage vs ~123 GB/year and ~1,230 GB, and the ~10,000 home-deliveries family vs the 17,200/month W19 canon; the profile's own §6.5 PO band was left against its restated §15.1) — the retired forms banned on live PA/README surfaces, footer-stripped bodies and the classification register's live cells, the calibrated anchors required at the repaired cells, the gap-analysis scenario tables and version footers exempt as frozen history; teeth: five synthetic injections each caught at its exact arm; the same day's sixty-seventh-wave review extended calibrated_volume_hits for the sibling-cell stragglers the calibration and the wave both left inside the very files they repaired (W4's own Volume row + four staffing sentences at ~167/day, ~42/DC/day, ~50 lines/order under a Frequency row the wave itself re-pointed; PA-04.2's other six volume-citing workflows — W1352 Frequency vs its own re-based Volume, W1353 ~33+~115, W1369 ~148, W1370 ~5,000/month vs its own re-based Background, W836's 4×~10=~40 loads/day vs its own ~1,800–2,400/month clause; PA-04.3's dock-appointment cells at 30 outbound loads/DC/day vs the 60–80/day chain-wide canon; W1256's Volume at ~5,000 RTP shipments vs its own re-based Background; W277/W461's ~10,000–17,000-home-delivery / ~12,000–20,000-sum freight-billable family, the retired sum form file-scoped banned; W919's Background tail at ~5,000+ movements vs its own repaired Frequency; W784's ~60,000/year annual form; W1415's ~500–800 SKUs/delivery lines-per-drop; PA-10.2's staffing sentence) — all trued to the load/drop canon (one store-delivery load = one consolidated drop; 60–80 loads/day chain-wide = ~1,800–2,400/month = 15–20/DC/day; 6–10 truck trips/DC/day at 2–3 drops/trip; W4 at ~1,700 orders/day and ~3 lines/order → ~1,250 pick lines/DC/day) and anchor-pinned; teeth: three synthetic injections each caught at its exact arm; the same day's sixty-eighth-wave review extended calibrated_volume_hits for the calibration's own written-cell drift and the new canon rows' unread derived cells (W1352's Time Estimate written by the calibration at ~5,300/~1,060/~265 chain-wide pick lines against its own ~1,250/DC/day Frequency anchor → ~5,000, hours re-footed; W92's adjustment cells at ~1,700–2,600/month against the §1.1 ~30/day IAD row, re-based to 200×~3–5 + 4×~20–30 = ~680–1,120/month; W270's RTP cell at ~40 receipts/day/DC against PA-04.1's canon-citing ~50; W3962's retired ~1,400–1,600 POs/month band, trued to ~1,200 merchandise POs/month with all types at ~1,600–1,900; PA-28.2's two remaining 1,000 GB cells against its own ~1,230 GB anchor; W109/W666's 20–50-lines-per-delivery cells against the 60–90-line consolidated-drop canon; the wave-66 '5,000 replenishment' banned form narrowed to '5,000 replenishment orders' after firing on the corrected anchor) — teeth: four synthetic injections each caught at its exact arm, fixtures restored sha256-verified byte-identical; the same day's sixty-ninth-wave review trued the calibration straggler one layer further out (the root README's at-a-glance PO row still at the retired ~1,400–1,600 total-PO band — the one volume-citing surface outside both the PA/README glob and the versioned-doc list; trued to the §15.1 canon ~1,600–1,900 total production-calibrated, calibrated_volume_hits gaining the root-README arm with the '~1,400–1,600 total' form banned and the anchor required, the loose numeral pair exempt as PA-79.2's separated-employee band) and moved the §4-resolution-37 Asset-Tracking entitlement flag (L11496) from the D6 POS row to the F7 row its resolution record declares (ebs_blueprint_hits' flag-placement arm: the flag forms banned on D6, the anchor required on F7) — teeth: four synthetic injections each caught at its exact arm, fixtures restored sha256-verified byte-identical; the same day's seventieth-wave review trued the receipt/ASN/container sibling family one derivation step out from the pinned cells (W3's '~15 lines per receipt' Volume — the retired ~15-lines-per-PO basis, ×6,000 receipts/month implying ~90,000 receipt lines/month against the ~12,000 PO-lines/month canon; trued to ~2 lines per receipt; W3C's '~20–30 import containers per DC per month' against W1271's own ~100–150/DC/month and W1169's ~20–30/DC/week at the ~400–600 TEUs/month §7.1 canon; trued to ~100–150, PA-04.1-file-scoped banned; W1365's '~4,800 ASN submissions/month' = 80% × the receipt canon against W1006's 1-ASN-per-PO ~700–750/month and PA-03.4's ~5,000–10,000 combined portal-document band, its own 720–960 exceptions equaling W1006's entire ASN universe; trued to ~700–750 ASNs, ~105–150 exceptions, hours/staffing re-footed; W1006's 'average 15 lines per ASN' under its own 1-ASN-per-PO model; trued to ~7 lines per ASN; W1169's '~15–25 container drayage moves per DC per week' below its own ~20–30 containers/DC/week Frequency; trued to ~20–30 round-trip moves; plus the rule docstring's duplicated sixty-eighth-wave paragraph removed) — calibrated_volume_hits' seventieth-wave arms ban the four retired forms repo-wide plus the container form file-scoped and require the corrected anchors at the six repaired cells; teeth: five synthetic injections each caught at its exact arm, fixtures restored sha256-verified byte-identical; the same day's seventy-first-wave review trued the returns canon — the one §1.1 row the production calibration grounded (14,744/13,584 AR credit memos/year at ~1.05 lines/doc → ~40/day ≈ ~1,200/month) but whose workflow family it never re-derived: ~50 cells across 16 documents moved (W1622's master to ~950–1,050 counter returns = the canon less W1629's ~100–150 B2B credit notes and W215's ~50–100 courier pickups; the ecommerce sub-canon to ~250–350 requests at ~0.6–0.8% with ~200–250 drop-offs and a ~50–100 courier remainder; the derived shares to ~325 exchanges/~180–215 no-receipt/~180–215 quality/~875 refunds; W1629 to ~100–150 B2B returns; W509/W540/W605/W1336/W707/W2308/W101/PA-30.2/PA-80.2 and the rate cells at PA-118.2/PA-125.2/PA-149.3/PA-157.2; the profile §5 bullet to ~0.04% (profile bumped); the custody register's E-09 row and erp-requirements GOV-051 bumped; hours and staffing re-footed throughout, all absorbed, no HC change; category-rate prose and the '~56,000 hours' training population exempt) — calibrated_volume_hits' seventy-first-wave arms ban the retired forms on live surfaces and require the corrected anchors at the repaired cells plus the custody register's live-body E-09 anchor; teeth: four synthetic injections each caught at its exact arm, fixtures restored sha256-verified byte-identical; the same day's seventy-second-wave review trued the dodging-variant stragglers — the three families the prior sweeps' banned forms read past at the glyph level: the hyphenated '~5,000 store-replenishment orders/month' on PA-136.1's W4126 Volume + the VS-136 README framing sentence (both wave-66 banned forms are space-form) with W4126 step 2's surviving 'grouping 2–3 orders/truck' consolidation mechanic, the returns-canon sibling stragglers inside the very files the seventy-first wave repaired (PA-32.3's W1641 '~56,000 return dispositions/month' Frequency + touchpoint + 'thousands of returns' pain point against its own trued ~1,200/month Time Estimate; W1640's refurb pool ~500–800 items/month = 42–67% of ALL returns re-based to the ~180–300 damaged-packaging-functional share, ~45–75/DC, ~900–1,500 h/yr; PA-23.3's W1336 Background at '~56,000 product returns per W12/W1627' with 'PHP 60–100M' losses from the retired fraud universe — trued to ~1,200 and ~PHP 3–6M/yr — and step 1(f)'s '>5% of transactions (~2.5x the chain average)' on the retired ~2% basis, trued to the ~0.04% canon), and the AP business-day family review #60 left behind (W7's own Time Estimate at the retired 305–370 band — ~25–31 h scanning, ~70 invoices × 25 min, ~60–70 h total — with its staffing tail's ~20–30-h/~53–67-h/~5–6-h derivates, and PA-22.1's verbatim copy; all trued to 395–440: ~33–37 h + ~33–37 h at 79–88 invoices = ~68–76 h, 10 clerks ~7 h each) — calibrated_volume_hits' seventy-second-wave arms ban the hyphenated form, the 56,000-dispositions/monthly-returns/product-returns forms, the consolidation-mechanic form, the refurb-pool and W1336 forms, and the 305–370 band with its derived hour cells, and require the corrected anchors at the repaired cells; teeth: four synthetic injections each caught at its exact arm with file and line named, fixtures restored sha256-verified byte-identical; the same day's directed virtual-gemba cadence-ladder fix extended gap_fill_straggler_hits with the tool-source arm — the calibration's compound 'chain-wide (per-store)' Frequencies had the time-and-motion tool's rule-1/rule-2 bridges crossing the parenthesis (W2201 read 21.84M events/yr, 840× high; the re-run's receiving-clerk ×12.9 demand jump was parser artifact, not workload), the paren-crossing bridge class banned in virtual-gemba-walk.py, the paren-blocked class required exactly ×4 with the docstring measurement-assumption bullet pinned; the seventy-fifth-wave consistency review added atlassian_suite_hits — the A6.6 Atlassian canon's completeness guard: the Atlassian Guard identity-plane forms required at the A6.6 row, the Design-Decisions row and guardrails 5/7, the completed 16-workflow Source cell (W495/W5521 joined), the guardrail 2/4 built-product scoping with the EBS ADOP online-patching boundary, the Bitbucket branch-permission form, the two canonical data-volumes §3 matrix rows and their 02-oracle-ebs pattern-register mirrors (endpoint order stays with integration_mirror_hits), the W152 Guard-provisioning and W132 branch-permission touchpoint anchors, the W367 Guard audit-log anchor, the sourcing model's §8 Atlassian vendor-commodity line + §12.2 'in capability sourcing' true-up, and the retired forms banned (the universal 'every production change carries the chain' and Guard-less 'cloud audit logs streamed to the SIEM' forms doc-scoped on the assumptions doc; the incorporation pass's 'Jira/ServiceNow'/'TestRail'/'(Confluence, SharePoint, or ITSM built-in KB)' hedges repo-wide across the authored trees). 2026-09-23 seventy-sixth-wave review: domain_companion_hits joins the guard — the batch-30 (W5580, corpus 5,432 → 5,433) census stragglers on the surfaces no arm read: the value-stream-index H1 banner and closing Total footer (both maintained live at batch 26, both stranded at 5,432), the conformance canon's own §1 framing sentence (canon bumped v1.5, the conformance guard's DOC anchor re-pointed), the IT domain companion's Technology-&-Data roster (393 → 394 / VS-113 (31) → (32) — Check 76's own class one derivation step outside its four quoted cells), and the retired 6,762 employee canon on the IT/operations/people companions' §1 scope lines and evidence cells (the twenty-ninth-wave headcount sweep covered the PA files and VS READMEs but never opened the four domain companions) — retired forms banned doc-scoped, the corrected anchors required at the repaired cells, The 2026-09-23 (x) trade-desk disablement added the CAP-B01–B04 estate anchors (registry §3.4 rows, the assumptions Design-Decision row, the TO's disabled annotations, the profile §3.3 bullet) to domain_companion_hits — the Check-71 CENSUS-pin contract; the seventy-seventh wave trued the (x) sweep's own residue and closed the guard's own false-pass — the blueprint README §4 W4 row's retired '6,932 employees' (ebs_blueprint_hits arm (l): the retired form banned on the footer-stripped body, the '6,925 employees' active anchor required at the W4 row), the AI-first guide §1.4's 'HQ 532 … 6,932 employees' with the 09-18 edit's mangled '— was 2026-09-14' fragment (guide_figure_hits arm (f): both retired forms banned, the 'HQ 525 active' / '6,925 employees (TO design 6,932)' anchors required; §6.6's dated promotion arrows exempt as frozen history), and profile_derived_figure_hits' two-base quotient tolerance scoped per surface kind — the (x) settlement made the TO two-state row's design column (6,932) compute to the retired active quotient (8.99M), so PA-133.3's half-repaired Volume cell ('~6,925 employees … target ~PHP 8.99M') passed as target-state; PA/VS-README citations now require the active canon, companion surfaces may still cite the design quotient |
| [audit-enrichment-completeness.py](audit-enrichment-completeness.py) | Mitigation-clause and Trigger-richness completeness guard (2026-08-29 enrichment pass) — errors on risk bullets without mitigation semantics and cadence-only Trigger values outside the shared-event allowlist. Companion to validator Check 58 |
| [audit-risk-labels.py](audit-risk-labels.py) | Risk-label punctuation guard (2026-08-29, consistency review #40) — retires the em-dash risk-label form normalized to the colon form. Companion to validator Check 57 |
| [audit-operational-controls.py](audit-operational-controls.py) | Controls-section operational-prose variant guard (2026-08-29, consistency review #39) — retires the gate-sentence variant forms normalized by the review (hyphenated 'review-and-approval gate', 'review & approval', 'VP for …', '(CSR)' gloss, '(HQ)' qualifiers). Companion to validator Check 56 |
| [audit-participants-units.py](audit-participants-units.py) | Participants-field hygiene and per-unit volume coherence guard (2026-08-29, consistency review #38) — errors on RACI markers inside Participants rows, the 'Analytics Mgr' abbreviation, and Volume/Frequency rows whose per-DC/per-store figure contradicts the stated chain-wide total. Companion to validator Check 55 |
| [audit-field-vocabulary.py](audit-field-vocabulary.py) | Pain-Points risk-label, Frequency-cadence, and Owner-role vocabulary guard (2026-08-29, consistency review #37) — retires the thirteen variant risk labels, the unhyphenated ad-hoc form, and the EHS-Manager spelling normalized by the review. Companion to validator Check 54 |
| [fix-auto-keywords.py](fix-auto-keywords.py) | Automation-keyword repair + RACI role-title normalization (2026-08-29, consistency review 36) — repairs the defragment-automation.py extractor's keyword glitches against each bullet's own referenced step (mid-word clips like logy re-quoted from the step's full word, Logistics Inc.-style legal-entity captures re-quoted with the step's true object, trailing fragment trims, nested-quote extraction, case normalization), and normalizes 41 cell-bounded RACI role-title variants to their dominant forms. --check is the zero-false-positive guard enforced by validator Check 53 |
| [audit-st-touchpoints.py](audit-st-touchpoints.py) | System-Touchpoints vocabulary and Trigger-duplication guard (2026-08-29, consistency review #35) — retires the eight spelling/hyphenation variants the review normalized to the dominant house forms, and errors on any same-PA byte-identical Trigger pair outside the 20-cluster adjudicated shared-event allowlist (parallel program streams, calendar-cadence siblings). Companion to validator Check 52 |
| [reconcile-staffing-claims.py](reconcile-staffing-claims.py) | Headcount-anchor and Volume-product reconciliation (2026-08-29, consistency review #34) — checks staffing-team and role-count claims in PA prose against `model-company-profile.md` §3.3 (18 HQ departments = 362), §4 (200 × 29 stores, 4 × 150 DCs, 6,762 total) and §13.1 (Merchandising 40: 5 CMs, 10 Buyers, 5 Planners, 4 Pricing Analysts), retires the literals superseded by the review's repairs, requires any '<Department> team of N' claim to quote the canonical §3.3 total (engagement crews exempt), and verifies every explicit A × B = C product inside Volume/Frequency field rows elementwise (K/M endpoints; week→month cadence allowance; '+'-sum rows out of scope). The 2026-09-14 twenty-ninth-wave review re-based the register to the promoted 511-HQ §3.3 (re-based by the promotion session itself) and retired the pre-promotion HQ-subtotal family its 6,762-total sweep had left behind — the '362 HQ'/'HQ = 362'/'362 staff' forms, the old six-function 'HSE 10, Quality 4…' enumeration and the 5-person trade register — across the PA files plus a literal-only pass over the VS READMEs (PA-22.1's HQ-Departments table, PA-72.3/PA-138.2/PA-19.3 Volume rows, PA-34.1/PA-40.2 cells, PA-13.1 and VS-169's README re-cut to the promoted register in the same pass). The 2026-09-23 (x) trade-desk disablement re-based the active canon 6,932 → 6,925 (252 live cells across 98 PA/README files swept) with the retired-6,932 form banned on live PA/README lines by the new retired-headcount arm — the Check-71 CENSUS-pin contract. Companion to validator Check 51 |
| [virtual-gemba-walk.py](virtual-gemba-walk.py) | Virtual gemba walk & time-and-motion analyzer (2026-09-16, thirty-ninth wave, by direction) — read-only decision-support over the workflow corpus's own measurements. `walk` mode: per-value-stream process floor — human/automated step mix, designed touch time per event (step-duration midpoints), role→role handoff density, approval-gate density, and the corpus's busiest role→role handoff paths. `motion` mode: per-role annual demand hours (step-duration share × events/year via a documented cadence ladder — explicit 'N–M per period' phrases, bare-cadence words store-scaled ×200, workday-counted for HQ/DC; 53% frequency-parse coverage) vs chartered capacity (TO §5.3 register HC for HQ; §7.2 ×200-store and §7.3 ×4-DC roster complements for the field; 1,800/1,900 net productive hours assumptions), with utilization bands, hot/cold flags and unmapped-demand diagnostics. Population-pinned (5,433 workflows / HQ 532 / stores 5,800 / DCs 600 — mismatch exits 1; HQ re-based 2026-09-18 actual-org gap-fill, workflow canon re-pointed at the 2026-09-21 batch-26 gap fill); interpretation guardrails printed with every run (R-cell naming is accountability, not timesheet actuals; the wave-31 exercise-through-broader-titles contract applies to narrow titles; unparseable-cadence workflows are excluded from annualized figures). Not wired into validate-repo.sh — analytical baselines legitimately move with gap-fill batches; 2026-09-23 paren-blocking fix — the calibration's compound 'chain-wide N–M per period (n–m per store per period)' Frequencies (6 cells, e.g. W2201's '~1,800–2,400 drops/month (2–3 per store per week)') had the ladder bridging the chain-wide count across the parenthesis into the per-store tail (840× error; the corrected W2201 events ~26,000/yr agree with the chain-wide clause's ~25,200/yr — the calibration internally consistent, the parser was not), bridges now paren-blocked and the corrected motion baseline: receiving clerk 102% / store manager 1,473% with hot/cold membership unchanged pre-vs-post calibration (the calibration itself demand-neutral; guard: gap_fill_straggler_hits cadence-ladder arm) |; fifty-sixth-wave store-scope extension (2026-09-18): bucket-preferred singular/plural capacity folding (store vs DC same-noun rosters) and duration-period qualifiers ('30 min/month' → x12/yr, not x store-events) + fix-store-role-scope.py's corpus true-up feeding it
| [audit-time-estimate-math.py](audit-time-estimate-math.py) | Arithmetic audit of the finalized `### Time Estimate` / `### Staffing Implication` paragraphs (2026-08-29) — re-derives every explicit inline `A × B (= or ≈) C` chain under the unit conventions the house style licenses (min↔hours, sec→hours, workday/workweek/month-length alternates, per-cadence annualization ×12/×4/×52/×365/×6, noun/suffix cancellation, canonical 200-store and 4-DC scalings, shared-factor inheritance, K/M endpoints, %-as-decimal), flagging endpoint mismatches and reversed ranges. Default mode lists every candidate for human adjudication (house-style range padding, elapsed-window weeks, hidden context factors are accepted classes); `--guard` is the strict zero-false-positive subset enforced by validator Check 50. The 2026-09-23 seventy-third-wave review triaged the full default-mode queue (183 candidates) into those accepted classes, repaired twelve claimed-total footing defects across nine PA files (W599, W634, W1474, W1833, W1852, W2169, W2171, W2172, W2179, W2670, W2673, W2674 — each claim now deriving from its own stated factors), pinned the twelve cells with the new workflow-scoped footing-anchor scan (required corrected claims, retired forms banned, both modes), and opened W251's benefits-staffing total (~60–80 h/month vs its own ~380–550 h/month steps-table derivation) as a tracked HC-cascade triage item |
| [finalize-time-estimates.py](finalize-time-estimates.py) | One-time closure of the mechanical draft Time Estimates (2026-08-28) — auto-decides the unambiguous drafts (uniform per-occurrence Durations × explicit Frequency counts) and renders the rest from a hand-adjudicated decisions file, every figure derived from the workflow's own steps/Frequency/Volume (per-unit Volume scaling, own-cadence steps, elapsed timelines reported not summed). Companion to validator Check 49 |
| [fix-headcount-6757.py](fix-headcount-6757.py) | Stale total-headcount figure repairer — propagates the 2026-06-25 headcount change (`6,757 → 6,762`) into the workflow catalog body, skipping legitimate historical `X → Y` change-notes and `workflow-gap-analysis.md`; companion to validator Check 24 (Part B) |
| [fix-toc-anchors.py](fix-toc-anchors.py) | Intra-file TOC anchor repairer — regenerates each PA file's `## Workflows in This Process Area` navigation anchors from the (correct) display text using GitHub's slug rules (github-slugger semantics, v2). Fixed the 1,926 malformed anchors (dropped hyphens, stale W-numbers) across 271 PA files; the 2026-06-26 review #11 pass corrected its slugger to GitHub's non-collapsing semantics and repaired a further 4,618 anchors whose spaced punctuation (` & `, ` / `) must render as a double hyphen. Idempotent; companion to validator Check 23 |
| [fix-table-structure.py](fix-table-structure.py) | One-time table-structure repairer — rewrites markdown delimiter (separator) rows to match their header's cell count, collapses doubled delimiter lines, and removes the stray single-cell `\| **Steps** \|` data row (18 fixes across 14 PA files; GFM does not recognize a table whose delimiter row mismatches the header, so those blocks rendered as plain text on GitHub). Idempotent, code-fence & escaped-pipe aware; companion to validator Check 29 |
| [fix-toc-completeness.py](fix-toc-completeness.py) | PA TOC hygiene repairer — deletes stray mid-file TOC fragments (nav lists left inside workflow bodies by merged edits; 8 lines in VS-04 PA-04.2 / VS-06 PA-06.2), de-duplicates TOC entries, and inserts TOC lines for shipped `## W…` headings that lack one (VS-12 PA-12.2: W1318), regenerating anchors with GitHub slugger semantics (`### W…` parent/summary sub-workflows are deliberately not TOC-indexed). Idempotent; companion to validator Check 30 |
| [fix-pa-names.py](fix-pa-names.py) | PA-name 3-way aligner — canonicalizes process-area names across the PA file H1, the VS README 'Process Areas' row, and the value-stream-index bullet (canonical source = index) after review #18 found 38 drifts (37 shortened H1s + 1 VS-README outlier). Idempotent; companion to validator Check 31 |
| [fix-pa-wrefs.py](fix-pa-wrefs.py) | One-time repairer of dangling & misdirected cross-references inside PA-file workflow bodies — review #20 found 19 dangling `W…` tokens (zero-padded `W01`/`W03`/`W05`, a PA reference written in the W namespace (`W03.4`), phantom `W413`/`W1593`/`W6796`, slug-style `W05.2`), 2 references that resolved to the wrong workflow (`W1593`→W706 supplier scorecard, `W2347`→W2469 scorecard feed), and 6 hand-written `links to VS-X` bullets citing CTL ids owned by unrelated process areas (CTL-357/335/689 → CTL-349/304/614). Explicit verified remap table; idempotent; companion to validator Check 33 |
| [fix-ctl-pa-names.py](fix-ctl-pa-names.py) | One-time canonicalizer of the CTL-240–808 PA operating-control objective names on both surfaces — `add-pa-controls.py` had derived them from PA file slugs (mangling proper nouns: `S&OP`→`Sandop`, `DENR`→`Denr`, `ERP`→`Erp`, `DC`→`Dc`, `B2B`→`B2b`, `CIP`→`Cip`, `FP&A`→`Fpanda`, dropping words) and `backfill-controls.py` truncated the PA-body mirror at 90 chars mid-word (145 instances). Rewrites 540 register rows + 3,592 PA-body parentheticals to the canonical value-stream-index PA name. Idempotent; companion to validator Check 34 |
| [fix-vs-wrefs.py](fix-vs-wrefs.py) | One-time repairer of workflow references written in the VS namespace inside workflow-catalog files — review #21 found 87 citations across 25 PA files of the form "(links to `VS-469` DTI)" where the number is not a value stream at all but a workflow id (`VS-469`→W469 DTI escalation, `VS-289`→W289 pricing master, `VS-245`→W245 vendor chargebacks, `VS-4398`→W4398 wayfinding, …); every target gloss-verified before remap (incl. one context-specific case: "`VS-292` service/non-stock master" → W296, not employee-master W292). Idempotent; companion to validator Check 35 |
| [fix-controls-bullets.py](fix-controls-bullets.py) | Controls-section rendering-defect repairer — review #27 found 3,349 Controls sections whose `operational:` items were emitted without a bullet prefix (rendering the operational controls outside the markdown list), 78 Controls lines with unbalanced parentheses (the generator's pain-point regex truncated captures at "e.g."/"vs."/"No." periods and swallowed `)` at `; operational:` joins), 35 lines carrying pain-point description fragments mislabelled as controls (repaired surgically from each workflow's own intact Pain Points — genuine mitigations kept verbatim, junk dropped), 11 Pain-Points bold labels closed with a stray `"` instead of `**`, and 1 Pain-Points bullet written as a table row (PA-137.2). Idempotent; companion to validator Check 43 |
| [fix-controls-splice-artifacts.py](fix-controls-splice-artifacts.py) | Forty-ninth-wave (2026-09-17) Controls-splice repairer, mechanical classes — 50 exact-match repairs across 43 PA files: duplicated `operational:` items (the later copy drops the item's leading modifier — "mandatory X; operational: X") and dangling empty tails ("...; ."), both minted by the semantic-batch Controls enrichment; every repair is an exact-match assertion so re-runs are safe. Companion to fix-controls-splice-artifacts-2-context.py; guarded by validator Check 21 arms (f)–(i) |
| [fix-controls-splice-artifacts-2-context.py](fix-controls-splice-artifacts-2-context.py) | Forty-ninth-wave (2026-09-17) Controls-splice repairer, context class — 49 exact-match repairs across 29 PA files: pain-point risk clauses and mitigation-clause substrings minted as `operational:` control items, and item lists truncated at "vs." (the mint pass split items on sentence periods); every replacement re-grounded in the owning workflow's own Pain Points/Steps text. Companion to fix-controls-splice-artifacts.py; guarded by validator Check 21 arms (f)–(i) |
| [fix-ebs-vehicle-stragglers.py](fix-ebs-vehicle-stragglers.py) | Fifty-second-wave (2026-09-17) EBS-exhaustion straggler repairer — 87 exact-match ST/step/Pain/Controls bullets across 41 PA files re-pointed to their adopted vehicles (Oracle eAM, Oracle Quality, Demantra, XTR/CE, AGIS, Procurement Contracts, Product Hub/SLM, Property Manager + the A12 EXT, iRecruitment, Advanced Pricing, In-Memory Cost, TCA DQM/SLM, OLA, Payroll PH, the Route Optimization Engine/OTE, Oracle Alert); adjudicated-legitimate mentions (plain-word EBS module naming, domain-label cross-references, build-posture naming) left standing; the retired forms guarded by Check 46's vehicle_literals extension. With part 2 and the wave's 23 inline context repairs the sweep totals 132 repairs across 61 PA files |
| [fix-ebs-vehicle-stragglers-2.py](fix-ebs-vehicle-stragglers-2.py) | Fifty-second-wave (2026-09-17) EBS-exhaustion straggler repairer, part 2 — whole-corpus scan beyond the ST sections: 22 further exact-match repairs (CMMS residuals → eAM, the 'ERP loyalty module' doctrine conflicts → the in-house stack per D9, revenue-recognition/subscription rows → RM&I, rebate rows → Trade Management, succession → Succession Planning, compliance-case → ICM, quality-inspection → Oracle Quality, yard → WMS Yard, sourcing → Sourcing, warranty → Install Base); forms guarded by Check 46 |
| [fix-store-role-scope.py](fix-store-role-scope.py) | Fifty-sixth-wave (2026-09-18) store-layer role-scope true-up — 93 structured-cell edits from the store consistency & time-and-motion review: 66+34 store 'Customer Service Representative' → store-roster 'Customer Service Rep' (Owner/Participants/Role cells + prose in the 9 unambiguous store-execution files; HQ contact-center workflows keep the HQ form), 14 'Floor Associate' role cells → Sales Associate (PA-09.1/09.3/24.2), W562's 9 pure LP-Officer daily-routine cells → Store Manager per the workflow's own no-dedicated-officer Owner clause, and 4 dual-scope Frequency fields re-worded to lead with the per-store figure (W559/W1168/W566/W609); companion to the virtual-gemba-walk bucket-aware capacity fix and the store_scope_hits guard |
| [fix-pain-template-engine.py](fix-pain-template-engine.py) | Fiftieth-wave (2026-09-17) Pain-Points de-templating engine — shared by the three fix-pain-template-vs*.py drivers: derives the template pool corpus-wide (bullets shared verbatim by ≥3 workflows), replaces only pool bullets in each targeted workflow's Pain section, preserves any specific (non-pool) bullet in place, skips sections already fully specific, and verifies paren/quote balance plus mitigation semantics on every new bullet |
| [fix-pain-template-vs65.py](fix-pain-template-vs65.py) | Fiftieth-wave (2026-09-17) Pain-Points de-templating driver, VS-65 (Marketplace Integration) — 24 workflow-specific Pain sections (48 bullets) authored from each workflow's own steps: commission drift at onboarding, oversell windows in the 15-minute sync, POD-gated invoicing, dispute-window discipline in settlement reconciliation |
| [fix-pain-template-vs66-68-74.py](fix-pain-template-vs66-68-74.py) | Fiftieth-wave (2026-09-17) Pain-Points de-templating driver, VS-66/68/74 (Project Design, Trade Credit, Jobsite Delivery) — 72 workflow-specific Pain sections (145 bullets): measurement-error propagation, model drift and matrix bypass in credit, crane-site suitability and evidence-based damage resolution in jobsite delivery |
| [fix-pain-template-vs75-77-78.py](fix-pain-template-vs75-77-78.py) | Fiftieth-wave (2026-09-17) Pain-Points de-templating driver, VS-75/77/78 (Digital Engagement, Construction Staging, Green Building) — 72 workflow-specific Pain sections (145 bullets): staged-rollout stalls and opt-out spirals in the app program, price-lock escalation caps and reservation cannibalization in staging, certification-forge and greenwashing-exposure risks in green building |

## Future Additions (Post-Platform Selection)

The following documents are out of scope for the current planning phase and will be developed
during implementation:

- **Implementation approach & phasing** — Wave/go-live strategy aligned with Tier 1/2/3 criticality
- **Testing strategy** — SIT, UAT, integration testing, parallel run approach
- **Change management plan** — Training curriculum, organizational readiness, adoption metrics
- **Data migration runbook** — Detailed cutover plan building on `01-model-company/data-migration-mapping.md`
- **Integration specification** — API contracts, message schemas, error handling per touchpoint
- **Security hardening guide** — Role matrix, SOD rules, penetration testing scope
- **Performance testing plan** — Load targets, batch window validation, peak simulation

---

*Date: 2026-08-28 (consistency review #33: statutory-citation correctness sweep #4 + house-spelling normalization — the validator was green at 48 checks going in, and this pass reversed one of review #31's own adjudications: the Philippine Competition Act is **RA 10667** (signed 21 July 2015 — the act that creates the PCC), so review #31's RA 10667 → RA 10677 flip was reverted repo-wide (RA 10677 → RA 10667 across 25 spots incl. VS-129 headings, index/dependency/touchpoint-map rows) and validator Check 46's retired-literal guard now retires the wrong number instead of the right one; six further mis-citations repaired — PA-100.1's 'ADR Act RA 876' → 'Arbitration Law (RA 876) / ADR Act (RA 9285)' label, PA-08.1's three 'age-restricted items per RA 7610' → 'RA 9211 §9 tobacco / PD 1619 volatile-solvent' spots (age-restricted sale bans, not the child-protection act), the COM-001 requirement row's 'DOLE DO 53-03' → 'D.O. 53-04' typo for the drug-free-workplace guidelines [Review #68, 2026-08-30: REVERSED — web-verified against DOLE's own download page and the Supreme Court E-Library that the drug-free-workplace guidelines for the private sector are D.O. 53-03, s. 2003; #33's '53-04' was a mis-verification that flipped only COM-001 + the VS-150 README against the corpus majority (CTL-129, W4458, PA-150.1/2/3, PA-22.2, PA-167.2, gap-analysis). The corpus now cites 53-03 and Check 46 retires the 53-04 form.], PA-17.3's 'RR 13-2018 (SAF-T)' → 'RR 8-2020' e-invoicing example (e-receipts run per NIRC §237 as amended by TRAIN; SAF-T is an OECD format, not a BIR standard), PA-31.3's 'PD 1586, RA 6969' → 'RA 6969 + IRR' release-notification pairing (PD 1586 is the EIS/ECC law), and VS-192's 'IRR (DAO 2023-05)' → 'DOE IRR' for EVIDA (a DOE-administered act whose IRR is a DOE department circular — DENR DAO 2023-05 is the EPR Act IRR); plus review #31's '(2020)' dating of the LPG Industry Regulation Act corrected to 2021 in the guard comment and the classification v7.40 footer, the absorbed v7.39 footer paragraph restored as a standalone note, and a house-spelling normalization sweep to the dominant US spellings (dependant → dependent 23 spots incl. the W4710 title/anchor/register-row cascade; judgement → judgment 9; fulfilment → 1; labelling → 3). Check 46's guard extended with the seven newly retired forms; 48 checks unchanged. Totals unchanged 188 VS / 569 PA / 5,363 WF / 728 Req / 808 CTL / 6,762 HC; validator green 0 errors / 0 warnings). Prior: 2026-08-26 (consistency review #32: hazardous-waste/chemical-safety citation sweep completion — review #31's DAO 2013-22 -> DAO 2004-36 adjudication extended to the 21 sibling surfaces across 7 PA files that its sweep missed, all found by re-auditing every remaining DENR/DOLE chemical-safety citation against the statute each is invoked for: one **spelled-out** longhand spot of `DENR Administrative Order No. 2013-22` (→ DENR DAO 2013-22 -> DAO 2004-36 under RA 6969) in PA-07.2 carrying the same wrong title and a residual US-style 90-day storage limit — the spelled-out form dodged both the review #31 fix sweep and the validator's retired-literal guard; an internal contradiction inside PA-22.1 itself (`typically 90 days maximum storage` on the accumulation-monitoring step vs the same file's post-#31 `6-month storage limit` risk line); seven phantom **DAO 2015-09** spots (PA-24.3 ×6, PA-25.1 → DAO 2013-22 -> DAO 2004-36) doing the Procedural Manual's job or mandating SDS, with the SDS mandate re-pointed to RA 6969 + implementing rules; PA-01.3's phantom toxic-substances SDS order `DENR Administrative Order 2015-08` (→ RA 6969 + implementing rules) paired with a dubious `DOLE Department Order 1989-114` OSHS promulgation (→ the OSH Standards per D.O. 198-18, repo-canonical since review #29); PA-07.2's paint-mixing penalty attribution to **DAO 2021-19** → RA 6969 / DAO 2013-22 -> DAO 2004-36; and PA-09.2's septic-tank sizing standards attributed to **DAO 2005-10** → the Sanitation Code (PD 856, Ch. XVII) / National Plumbing Code already cited repo-wide; plus six **short-form `DENR AO 2013-22`** (→ DAO 2013-22 -> DAO 2004-36) **spots** the first grep pass missed (PA-07.2 ×5 incl. a `DOLE Department Order 136-14` → OSH Standards per D.O. 198-18 mis-attribution — DO 136-14 governs construction-site OSH — and a GHS 16-section format attribution; PA-13.2 lead-free certification → DENR CCO 2013-24, VOC per PNS paint standards). Validator Check 46's retired-literal guard extended with all newly retired forms (48 checks unchanged). Totals unchanged 188 VS / 569 PA / 5,363 WF / 728 Req / 808 CTL / 6,762 HC; validator green 0 errors / 0 warnings). Prior: 2026-08-26 (consistency review #31: statutory-citation correctness sweep — the third pass over the repo's ~100 distinct Republic Act/PD/DO/DENR citations found five more mis-citations, this time in the numbers prior audits had themselves adjudicated or never opened: the LPG Industry Regulation Act is **RA 11592** (2020) — the act number went RA 10862 → RA 10617 → RA 11592 across reviews #13/#29/#31 (RA 10617 sits in the late-2013 numbering, predating the 17th/18th-Congress LPG bills), so the W5059 title/TOC anchor/register row and 19 live spots re-pointed again; the Philippine Competition Act number goes RA 10667 -> RA 10677 (25 spots incl. VS-129 headings, index/dependency/touchpoint-map rows); hazardous-waste management under RA 6969 runs on **DAO 2013-22 -> DAO 2004-36** — the citation goes DAO 2013-22 (mercury CCO) -> DAO 2013-22 -> DAO 2004-36 and a US-style 90-day accumulation limit had drifted into the DENR storage-limit machinery contradicting PA-22.2's own 'DAO 2013-22 -> DAO 2004-36 limits storage to 6 months' (re-based to 180-day across VS-73/PA-22.1/PA-114.3); BIR transfer-pricing benchmarking re-pointed from phantom RR 34-2022 -> **RR 02-2013** (PA-36.3); and VS-188's collateral-perfection citation of RA 9266 — the Architecture Act — replaced with Act No. 1508 chattel mortgage / RA 11057 PPSA. Validator Check 46's retired-literal guard extended with all five). Prior: 2026-08-26 (consistency review #29: validator Check 46 — stale-figure & superseded-citation literal guard — added alongside the semantic-drift repairs no structural check could see: W261's e-wallet Volume band rebased to the canonical tender mix (the stale 560,000-transaction × 30% inputs → 2.8M × ~15% = ~420,000 POS + ~17,000 ecommerce = ~437,000/month, harmonizing with W1268/PA-08.2), W1268's per-day conversion corrected (~14,000/day, was an order-of-magnitude slip), W265's POS hardware fleet re-derived to the canonical 600 terminals (3/store × 200; ~600 PM events and ~300 hours/quarter replacing the non-derivable 1,000/4,000/500 set, plus the same stale fleet figure in PA-29.3's barcode-sync step caught by the new check's first run), W478's BIR inventory-list volume re-pointed to the canonical SKU counts (35,000 active / ~55,000 item-master), the LPG Industry Regulation Act citation corrected repo-wide (19 spots: wrong act number → RA 10617, incl. the W5059 title + register row + TOC anchor), two 13th-month-pay citations corrected to PD 851, the phantom BIR ATC examples corrected to the real WC 010/WI 010/WP 010 set, and the store-forklift statute citation moved from the construction-industry DO 13-98 to the OSH Standards Rule 1160 series (per D.O. 198-18)). Prior: 2026-08-25 (consistency review #28: validator Checks 44 & 45 — root-README folder-tree agreement and anchored current-state total-figure agreement — added alongside the stale-figure repair: the root-README tree's 8 per-VS workflow counts left at pre-batch values by the post-catalog batches (VS-10/19/21/36/83/84 from batch 1, VS-24/31 from batch 2 — each batch reconciled the total line but not the directory rows), the tree's and format-guide's proposed-register descriptions (8/W5497–W5504 → 11/W5497–W5507), the format-guide 5,357-figure claims, the requirement-matrix 'full 5,357-workflow inventory' line, the VS-133 README/PA-133.1/PA-133.3 self-descriptions, and the dependency-map VS-133 row; plus removal of a stray mid-file 'Workflow Count: 31' footer stranded inside PA-10.1 (the only PA file carrying two footers) and derivability repairs to W5505's Volume and W5506's Frequency bands). Prior: 2026-08-25 (consistency review #27: validator Check 43 — analysis-section list hygiene + bold/paren balance — added alongside `fix-controls-bullets.py`, which repaired the rendering-defect cluster no prior check could see (all 42 predecessor checks validate counts/IDs/anchors/tables, none validated list hygiene inside analysis sections): 3,349 dangling non-bulleted `operational:` Controls lines, 78 unbalanced-paren Controls lines, 35 pain-point-description fragments mislabelled as controls, 11 mangled `**Label"` bold closes, 1 table-row Pain-Points bullet). Prior: 2026-08-24 (consistency review #25: validator Checks 40 & 41 added — (40) validator self-description agreement, repairing the two canonical self-descriptions that still said "35 checks" (root-README folder-tree line + the row above) although the script had grown to 39 across reviews #22–#24, and guarding every quoted check-count figure against the implemented `--- Check N:` marker count (per-version `*Date:` history notes exempt); (41) quoted requirement-total & category-count agreement, repairing executive-summary.md's "updated counts: 733 requirements" headline that predated review #22's five-duplicate removal (733 → 728) and guarding the 'N requirements across' / 'N unique requirements' / 'Total requirements: N' / Key-Metrics-row / 'across N categories' claim forms against the register). Prior: 2026-08-24 (validator Check 35 — VS-number citation resolution — added alongside consistency review #21, which repaired the workflow-reference-in-VS-namespace defect class no prior check could see: 87 citations across 25 PA files of the form "(links to `VS-469` DTI)" where the number is not a value stream at all but a workflow id — `VS-469` is W469 Customer Complaint DTI Escalation, `VS-289` the pricing master W289, `VS-245` vendor chargebacks W245, `VS-4398` wayfinding W4398 — every target gloss-verified and remapped by the new `fix-vs-wrefs.py`. Prior: 2026-08-24 — validator Checks 33 & 34 — PA-body/VS-README workflow-reference resolution and PA-control objective canonical-name agreement — added alongside consistency review #20, which repaired three defect classes no prior check could see: 19 dangling workflow-ID citations + 2 misdirected ones + 6 misdirected CTL citations inside PA bodies (Checks 6/7 had guarded only the four cross-reference docs), and the slug-derived/mid-word-truncated CTL-240–808 objective names on the register and its 3,942 PA-body mirrors — all fixed by the new `fix-pa-wrefs.py` and `fix-ctl-pa-names.py`. Prior: 2026-06-28 (validator Checks 30 & 31 — PA TOC hygiene and PA-name 3-way consistency — added alongside consistency review #18, which repaired three defect classes no prior check covered: 8 stray mid-file TOC fragments (VS-04 PA-04.2, VS-06 PA-06.2) invisible to Check 23 because anchors resolved fine while the TOC debris sat inside workflow bodies; 1 un-indexed workflow heading (W1318 in VS-12 PA-12.2) that Check 23 could not see because it only validates anchors that exist, never TOC completeness; and 38 process-area name drifts across the H1/VS-README/index triplet (37 H1s shortened at generation time, mostly in the VS-53–VS-63/152–187 Expansion and gap-analysis blocks, plus the PA-69.1 VS-README outlier) — all fixed by the new `fix-toc-completeness.py` and `fix-pa-names.py`. Prior: 2026-06-28 — validator Check 29 — repo-wide table delimiter/data-row column integrity — added alongside consistency review #17, which repaired 18 table defects no prior check covered: 16 delimiter rows with missing/excess columns, one doubled delimiter line (VS-29 PA-29.2), and one stray `| **Steps** |` row (VS-162 PA-162.3), all fixed by the new `fix-table-structure.py` — Check 13 had scanned only the summary docs and only data rows, so GFM-non-rendering tables in PA files shipped silently. Prior: 2026-06-28 — validator Check 28 — requirements-TOC letter-suffixed-ID & proposed-register-description guards — added alongside consistency review #16, which repaired the TOC's phantom `POS-022a` claim, the three stale 'remaining unclassified' navigation descriptions of `workflow-criticality-proposed.md`, and the `ecosategy` typo in CHANGELOG. Prior: 2026-06-28 — validator Check 27 — stale register-row figure & unclassified-workflow-claim guards — added alongside consistency review #15, which repaired the three prose figures the Full-Coverage Confirmation Pass left at their pre-pass values and executed the §8.2 VS-192 statutory-program incorporation the §8 coverage note had shipped past. Prior: 2026-06-28 — validator Check 26 — dependency-map §8 block-reconciliation guard — added alongside consistency review #14, which executed the §8 VS-192 incorporation v4.4 had flagged as pending. Prior: 2026-06-27 — `backfill-participants.py` added after the required-field completeness pass closed the last 645 missing instances; validator Check 22 green. Prior: 2026-06-19))* [Review #64, 2026-08-29: the DAO 2013-22 -> DAO 2004-36/180-day half of this note has been REVERSED — web-verified against the WTO notification, EMB's own dao-2013-22.pdf and the Japan-MOE register that DAO 2013-22 IS the Revised Procedures and Standards for the Management of Hazardous Wastes (RA 6969), revising DAO 2013-22 -> DAO 2004-36, with a 90-day generator on-site accumulation limit; the corpus now cites DAO 2013-22 and Check 46 retires the DAO 2013-22 -> DAO 2004-36 form.]  [Review #69, 2026-09-02: the tool table above was extended by reviews #34–#68 without further footer annotations (most recently review #68, which brought the target-state TO and the IT product operating model under Check 59's guard and updated these rows); review #69 added Check 63 (root-README worklist-row & methodology-tree guard), repaired the stale batch17 tree row in the root README, and re-pointed the two canonical check-count self-descriptions to 63 — see CHANGELOG. The #33 date above stands as the last dedicated methodology-docs consistency pass.]
